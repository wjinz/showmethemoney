# 📂 가계부 앱 프로젝트 핸드오버 및 기술 상세 보고서 (2026-04-05 17:34)

본 보고서는 '우진&지연 가계부' 프로젝트의 아키텍처, 핵심 로직, AI 연동 방식 및 최신 변경 사항을 상세히 기록하여 프로젝트의 원활한 유지보수를 돕기 위해 작성되었습니다.

---

## 1. 프로젝트 개요 및 기술 스택

### 🏗 기술 스택 (Tech Stack)
- **Frontend**: React 18, Vite 기반의 SPA (Single Page Application).
- **Backend/Hosting**: Vercel Serverless Functions (`/api` 디렉토리).
- **Database**: Supabase (PostgreSQL + Realtime 실시간 동기화).
- **AI Engine**: Google Gemini 1.5 Flash (v1 정식 버전).

### 📐 시스템 아키텍처
- **전역 상태 관리**: `src/App.jsx`가 중앙 허브 역할을 하며, `BudgetContext`를 통해 모든 뷰와 컴포넌트에 데이터를 공급합니다.
- **실시간성**: Supabase의 리얼타임 기능을 활용하여 남편과 와이프 기기 간 즉각적인 데이터 공유가 가능합니다.
- **오프라인 대응**: `offlineQueue` 유틸리티를 통해 인터넷이 없는 환경에서도 가계부 입력을 지원하며, 온라인 복구 시 자동 동기화됩니다.

---

## 2. 핵심 기능별 세부 구현 내용

### 📸 AI 스마트 스캔 (OCR)
- **작동 방식**: 사용자가 이미지를 선택하면 클라이언트(`src/utils/ocr.js`)에서 즉시 **1600px 리사이징 및 JPEG 변환**을 수행하여 업로드 속도를 높이고 API 에러(용량 초과 등)를 방지합니다.
- **서버 로직**: `/api/ocr.js`에서 Gemini 1.5 Flash API를 호출하여 영수증이나 카드 내역 이미지에서 JSON 데이터를 추출합니다.
- **UX 고도화**: `CardScanSheet.jsx`를 통해 스캔된 여러 건의 거래 중 사용자가 원하는 항목만 골라서 저장(`bulk input`)할 수 있는 검토 프로세스를 제공합니다.

### 💰 AI 자동 예산 계획
- **분석 로직**: `/api/budget-ai.js`에서 사용자의 월 수입, 고정비, 저축 목표를 기반으로 9개 카테고리에 최적의 예산을 배분합니다. 한국 평균 소비 패턴과 개별 사용자의 3개월치 지출 내역을 참고하여 개인화된 추천을 제공합니다.

### 📂 데이터 관리 최적화
- **연도별 분기 저장**: 지출 내역(`tx`)이 늘어남에 따라 발생할 수 있는 성능 저하를 막기 위해 데이터를 연도별 키(`tx_YYYY`)로 분리하여 관리하고, 필요한 연도의 데이터만 지연 로딩(Lazy Loading)합니다.

---

## 3. 필수 환경 변수 설정 (Deployment)

배포 환경(Vercel)에서 반드시 설정해야 할 변수 목록입니다.

| 변수명 | 용도 | 비고 |
| :--- | :--- | :--- |
| `GOOGLE_API_KEY` | Gemini 1.5 Flash API 인증 | Google AI Studio에서 발급 |
| `VITE_SUPABASE_URL` | Supabase 접속 주소 | Client-side 필수 |
| `VITE_SUPABASE_ANON_KEY` | Supabase 익명 키 | Client-side 필수 |
| `INTERNAL_API_SECRET` | 내부 API 보안 강화용 시크릿 | `/api` 호출 시 헤더 검증 |

---

## 4. 2026-04-05 주요 변경 및 장애 대응 이력

### ✅ 400/404 에러 원천 차단
- **배경**: 기존 Anthropic API의 잔액 부족(400) 및 잘못된 모델명 호출 이슈가 있었습니다.
- **조치**: 
  - AI 엔진을 **Google Gemini 1.5 Flash**로 전면 교체하여 무료 티어 활용도를 높였습니다.
  - API 경로를 `v1beta`에서 정식 안정 버전인 **`v1`**으로, 모델명을 **`gemini-1.5-flash-latest`**로 보정하여 모든 호출 에러를 해결했습니다.

### ✅ 빌드 성능 및 경고 해결
- `package.json`에 `"type": "module"`을 명시하여 Node.js 서버리스 함수의 ESM 호환성 경고를 해결했습니다.
- `vite.config.js`의 청크 크기 감지 한도를 높여 대형 패키지 포함 시의 빌드 로그 가독성을 높였습니다.

---

## 5. 유지보수 및 향후 제안
- **데이터 분석**: Recharts 라이브러리를 활용해 식비나 교통비 등 특정 카테고리 소비 추이 그래프를 추가할 수 있습니다.
- **엔진 업데이트**: Gemini 2.0 등 차세대 모델 출시 시 `/api` 폴더의 모델명만 변경하여 성능을 즉시 업그레이드할 수 있습니다.

---
**Antigravity Assistant 가 작성하였습니다.**
