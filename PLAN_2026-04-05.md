# 가계부 앱 버그 수정 및 개선 계획서

> 작성일: 2026-04-05  
> 작성 기준: 실제 소스 코드 전수 분석 (App.jsx, CardScanSheet.jsx, supabase.js, BudgetView.jsx, constants/index.js 등)

---

## 1. 현재 발견된 버그 목록 (우선순위 순)

| # | 심각도 | 위치 | 증상 |
|---|--------|------|------|
| B0 | 🔴 CRITICAL | `api/ocr.js:60`, `api/budget-ai.js:64` | Gemini API 404 — `v1` 엔드포인트에서 `-latest` alias 미지원 |
| B1 | 🔴 CRITICAL | `App.jsx:setTx` + `CardScanSheet.jsx:handleSaveAll` | 벌크 스캔 시 마지막 1건만 저장됨 |
| B2 | 🔴 CRITICAL | `App.jsx:setTx` (line 316-338) | 오프라인 중 tx 추가 시 큐에 저장 안 됨 (3회 재시도 후 소실) |
| B3 | 🟠 HIGH | `BudgetView.jsx:FixedTab` (line 86) | 고정비 신규 추가 시 `name` 필드 저장, `label` 기대하는 코드와 불일치 |
| B4 | 🟠 HIGH | `App.jsx:addTx` (line 376) | `Date.now()` 기반 ID → 동시 다건 추가 시 ID 충돌 |
| B5 | 🟠 HIGH | `supabase.js:subscribe` (line 123-141) | DELETE 이벤트 미구독 → `clearAllTransactions` 파트너 기기에 미전파 |
| B6 | 🟡 MEDIUM | `App.jsx:setShared` (line 293) | `addToast` 의존성 누락 → stale closure 경고/버그 |
| B7 | 🟡 MEDIUM | `App.jsx:setTx` (line 321-324) | tx 1건 추가 시 로드된 전체 연도 저장 (불필요한 Supabase 호출) |
| B8 | 🟡 MEDIUM | `HomeView.jsx:line 356` | 검색 시 전체 tx 순회 (연도 필터링 없음) |
| B9 | 🟢 LOW | `constants/index.js:48-52` | 모듈 로드 시점 고정 날짜 상수 (자정 이후 갱신 안 됨) |
| B10 | 🟢 LOW | `supabase.js` 전체 | realtime 구독 해제 후 재구독 시 채널 누적 가능성 |

---

## 2. 버그별 상세 분석 및 수정 코드

---

### B0 🔴 Gemini API 404 — `v1` + `-latest` 조합 불가

#### 원인 분석 (git 이력 기반)

```
commit ff51040 (AI 엔진 교체)
  → v1beta/models/gemini-1.5-flash          ← 동작했음

commit 3a2b6d5 ("404 해결"이라고 했지만)
  → v1/models/gemini-1.5-flash              ← v1으로 변경

commit 711de28 ("모델명 -latest로 변경")
  → v1/models/gemini-1.5-flash-latest       ← ⚠️ 여기서 404 재발
```

**현재 코드** (`api/ocr.js:60`, `api/budget-ai.js:64`):
```javascript
// 현재 (버그)
const geminiRes = await fetch(
  `https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash-latest:generateContent?key=${apiKey}`
//                              ^^                   ^^^^^^^^^^^^^^^^^^^^^^
//                           v1 정식               -latest alias는 v1에서 미지원
```

**왜 404인가:**
- Google Generative AI `v1` (stable) API는 **명시적 버전명**만 허용
  - `gemini-1.5-flash` ✅  
  - `gemini-1.5-flash-001` ✅  
  - `gemini-1.5-flash-latest` ❌ (v1에서 미인식 → 404 model not found)
- `-latest` alias는 `v1beta` 전용 기능

#### 수정 옵션 비교

| 방법 | URL | 특징 |
|------|-----|------|
| **즉시 수정 (최소 변경)** | `v1/models/gemini-1.5-flash` | -latest만 제거, v1 유지 |
| **권장 (안정적)** | `v1beta/models/gemini-1.5-flash-latest` | v1beta로 롤백, -latest 유지 |
| **최선 (업그레이드)** | `v1beta/models/gemini-2.0-flash-lite` | 최신 모델, 무료 티어, 빠름 |

#### 수정 코드 — `api/ocr.js:60`

**Option A: 즉시 수정 (1줄 변경)**
```javascript
// api/ocr.js:60 — -latest 제거
const geminiRes = await fetch(
  `https://generativelanguage.googleapis.com/v1/models/gemini-1.5-flash:generateContent?key=${apiKey}`,
//                                                                       ^^^^^^^^^^^^^^^
//                                                                       -latest 제거
```

**Option B: 권장 (v1beta + 최신 모델)**
```javascript
// api/ocr.js:60 — v1beta + gemini-2.0-flash-lite (OCR에 최적)
const geminiRes = await fetch(
  `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-lite:generateContent?key=${apiKey}`,
//                                             ^^^^^^        ^^^^^^^^^^^^^^^^^^^
//                                          v1beta로         현재 무료 티어 최신 모델
```

#### 수정 코드 — `api/budget-ai.js:64` (동일하게 수정)

```javascript
// api/budget-ai.js:64 — ocr.js와 동일하게 통일
const resp = await fetch(
  `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash-lite:generateContent?key=${apiKey}`,
```

#### 수정 후 공통 상수 관리 (권장)

두 파일에 모델명이 중복 하드코딩되어 있으므로, 수정 시 한 곳만 놓치는 일이 없도록:

```javascript
// api/_gemini.js (새 공통 파일)
export const GEMINI_MODEL = 'gemini-2.0-flash-lite';
export const GEMINI_URL = (model = GEMINI_MODEL) =>
  `https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent`;
```

```javascript
// api/ocr.js — import 후 사용
import { GEMINI_URL } from './_gemini.js';
const geminiRes = await fetch(`${GEMINI_URL()}?key=${apiKey}`, { ... });
```

#### 검증 방법

```bash
# Vercel 로컬 개발 서버에서 테스트
npx vercel dev

# 실제 영수증 이미지로 OCR 호출
curl -X POST http://localhost:3000/api/ocr \
  -H "Content-Type: application/json" \
  -d '{"image":"...base64...","mediaType":"image/jpeg"}' | jq .

# 예산 AI 호출
curl -X POST http://localhost:3000/api/budget-ai \
  -H "Content-Type: application/json" \
  -d '{"totalSalary":5000000}' | jq .
```

---

### B1 🔴 벌크 스캔 race condition — 마지막 1건만 저장됨

#### 원인 분석

`CardScanSheet.jsx:74-86` `handleSaveAll`이 `onSave`를 반복 호출:

```javascript
// CardScanSheet.jsx:74-86 — 현재 코드 (버그)
const handleSaveAll = () => {
  const selectedItems = items.filter(i => i.selected);
  selectedItems.forEach(i => onSave({     // ← 10건이면 onSave 10번 호출
    who, amount: i.amount, cat: i.cat,
    memo: i.memo, date: i.date,
    payMethod: 'credit', type: 'expense'
  }));
  onClose();
};
```

`onSave` = `addTx` = `t => setTx(ts => [...ts, { ...t, id: Date.now() }])`

`App.jsx:296-339` `setTx` 내부:

```javascript
// App.jsx:296-297 — 현재 코드 (버그)
const setTx = useCallback(v => {
  const newTx = typeof v === 'function' ? v(tx) : v;  // ← tx는 클로저의 스냅샷
  // ...
  setTxRaw(newTx);  // React 18 배칭 → 마지막 call만 적용됨
}, [tx, householdId]);
```

React 18 자동 배칭에 의해 같은 이벤트 핸들러 내 `setTxRaw` 10회 호출 시:
- 각 호출이 동일한 클로저 `tx` (N개)를 기준으로 `newTx` 계산
- `setTxRaw(tx+item1)`, `setTxRaw(tx+item2)`, ... → **마지막 `setTxRaw(tx+item10)`만 적용**
- Supabase에도 10개의 Promise가 병렬 실행되어 마지막 도달한 것이 덮어씀

#### 수정 방법

**방법 A: `setTx`를 functional update 방식으로 변경 (권장)**

```javascript
// App.jsx — setTx 수정 (핵심: setTxRaw를 functional update로)
const setTx = useCallback(v => {
  setTxRaw(prev => {
    const newTx = typeof v === 'function' ? v(prev) : v;  // prev = 최신 상태
    // 연도별 그룹핑 후 Supabase 저장 (비동기)
    const byYear = {};
    newTx.forEach(t => {
      const year = Number(String(t.date ?? '').slice(0, 4));
      if (!isNaN(year) && year > 2000) {
        if (!byYear[year]) byYear[year] = [];
        byYear[year].push(t);
      }
    });
    validate('tx', newTx);
    setSyncStatus("syncing");
    // 저장은 effect에 위임하기 위해 ref에 기록
    pendingTxSave.current = { byYear, ts: Date.now() };
    return newTx;
  });
}, [householdId]);
```

더 간단한 방법:

```javascript
// App.jsx — addTx만 functional update로 개선 (최소 변경)
const addTx = useCallback(t => {
  setTxRaw(prev => {
    const newItem = { ...t, id: Date.now() + Math.random() };
    const newTx = [...prev, newItem];
    // 현재 연도만 저장 (비동기)
    const year = Number(String(t.date ?? '').slice(0, 4));
    if (!isNaN(year) && year > 2000) {
      const yearTx = newTx.filter(tx => tx.date?.startsWith(String(year)));
      setSyncStatus("syncing");
      db.saveTx(householdId, year, yearTx).then(() => setSyncStatus("ok")).catch(() => setSyncStatus("error"));
    }
    return newTx;
  });
}, [householdId]);
```

**방법 B: CardScanSheet에서 배치 저장 (대안)**

```javascript
// CardScanSheet.jsx — handleSaveAll 수정
const handleSaveAll = () => {
  const selectedItems = items.filter(i => i.selected);
  if (selectedItems.length === 0) return;
  
  // 개별 onSave 대신 배열 전달용 onSaveAll prop 사용
  // 또는 단일 call로 묶기:
  const newTxList = selectedItems.map(i => ({
    who, amount: i.amount, cat: i.cat,
    memo: i.memo, date: i.date,
    payMethod: 'credit', type: 'expense',
    id: Date.now() + Math.random() * 1000 | 0  // 충돌 방지
  }));
  onSaveAll(newTxList);  // ← 새 prop, App.jsx에서 addTxBatch 구현
  onClose();
};
```

```javascript
// App.jsx — addTxBatch 추가
const addTxBatch = useCallback((items) => {
  setTx(prev => [
    ...prev,
    ...items.map((t, i) => ({ ...t, id: Date.now() + i }))
  ]);
}, [setTx]);
```

---

### B2 🔴 오프라인 중 tx 추가 → 큐 미등록

#### 원인 분석

`App.jsx:267-272` `setShared`는 `navigator.onLine` 체크 후 큐 등록:

```javascript
// App.jsx:267-272 — setShared는 오프라인 처리 있음 (OK)
if (!navigator.onLine) {
  enqueueOffline(key, newValue);
  setSyncStatus("error");
  return;
}
```

그러나 `setTx`(line 296-339)는 별도 구현으로 오프라인 처리 없음:

```javascript
// App.jsx:316-338 — setTx saveAll (버그: 오프라인 미처리)
const saveAll = async () => {
  let retries = 0;
  while (retries < 3) {
    try {
      const allLoadedYears = Array.from(loadedTxYears.current);
      await Promise.all(
        allLoadedYears.map(y => db.saveTx(householdId, y, byYear[y] ?? []))
      );
      setSyncStatus("ok");
      return;
    } catch (e) {
      retries++;
      if (retries === 3) {
        console.error(`[sync] tx 저장 실패 (3회 시도):`, e);
        setSyncStatus("error");  // ← 여기서 끝! 큐에 저장 안 함
      } else {
        await new Promise(r => setTimeout(r, 1000 * retries));
      }
    }
  }
};
```

#### 수정 코드

```javascript
// App.jsx — setTx 내 saveAll 수정
const saveAll = async () => {
  // 오프라인 즉시 큐 등록
  if (!navigator.onLine) {
    const currentYear = getYear();
    enqueueOffline(`tx_${currentYear}`, byYear[currentYear] ?? []);
    setSyncStatus("error");
    return;
  }

  let retries = 0;
  while (retries < 3) {
    try {
      const allLoadedYears = Array.from(loadedTxYears.current);
      await Promise.all(
        allLoadedYears.map(y => db.saveTx(householdId, y, byYear[y] ?? []))
      );
      setSyncStatus("ok");
      return;
    } catch (e) {
      retries++;
      if (retries === 3) {
        // 3회 실패 시 큐에 저장 (기존: console.error만 했음)
        const currentYear = getYear();
        enqueueOffline(`tx_${currentYear}`, byYear[currentYear] ?? []);
        console.error(`[sync] tx 저장 실패 → 오프라인 큐 등록`, e);
        setSyncStatus("error");
      } else {
        await new Promise(r => setTimeout(r, 1000 * retries));
      }
    }
  }
};
```

---

### B3 🟠 고정비 추가 시 `name` 필드 저장 (스키마 불일치)

#### 원인 분석

`BudgetView.jsx:86` `FixedTab` 초기 상태:

```javascript
// BudgetView.jsx:86 — 현재 코드 (버그)
const [newF, setNewF] = useState({ name: "", amount: "", cat: "housing", day: "" });
//                                  ^^^^  ← 'name'이 아닌 'label'이어야 함
```

`App.jsx:142-147` 로드 시 마이그레이션은 `cards`만 처리:

```javascript
// App.jsx:142-147 — cards 마이그레이션 (OK)
const normalizedCards = allData.cards.map(c => c.label ? c : { ...c, label: c.name || "미지정" });
```

`validate.js:30-35` fixed 스키마는 `label`을 검사하지 않아 경고도 안 나옴:

```javascript
// validate.js:30-35 — 현재 스키마 (누락)
fixed: (v) => {
  if (!Array.isArray(v)) return false;
  return v.every(
    (f) => typeof f === 'object' && f !== null &&
      'id' in f && 'amount' in f  // ← 'label' 검사 없음
  );
},
```

#### 수정 코드

```javascript
// BudgetView.jsx:86 — newF 초기값 수정
const [newF, setNewF] = useState({ label: "", amount: "", cat: "housing", day: "" });
//                                  ^^^^^  name → label 변경
```

저장/수정 로직도 함께:

```javascript
// BudgetView.jsx:88-96 — addF 수정
const addF = () => {
  if (!newF.label || !newF.amount || !newF.day) return;  // name → label
  const amount = parseInt(parseInput(newF.amount));
  const day = parseInt(newF.day);
  if (editFId) {
    setFixed(p => p.map(f => f.id === editFId ? { ...f, ...newF, amount, day } : f));
  } else {
    setFixed(p => [...p, { ...newF, id: Date.now(), amount, day }]);
  }
  setShowAdd(false); setEditFId(null);
  setNewF({ label: "", amount: "", cat: "housing", day: "" });  // name → label
};
```

validate.js도 강화:

```javascript
// validate.js:30-35 — fixed 스키마 강화
fixed: (v) => {
  if (!Array.isArray(v)) return false;
  return v.every(
    (f) => typeof f === 'object' && f !== null &&
      'id' in f && 'amount' in f && 'label' in f  // label 추가
  );
},
```

---

### B4 🟠 `Date.now()` ID 충돌

#### 원인 분석

`App.jsx:376`:

```javascript
// App.jsx:376 — 현재 코드 (버그)
const addTx = useCallback(t => setTx(ts => [...ts, { ...t, id: Date.now() }]), [setTx]);
//                                                              ^^^^^^^^^^^
// 같은 ms 내 여러 호출 시 동일 ID 생성 가능
```

#### 수정 코드

```javascript
// App.jsx:376 — ID 생성 방식 개선
const addTx = useCallback(t => setTx(ts => [
  ...ts, 
  { ...t, id: Date.now() * 1000 + Math.floor(Math.random() * 1000) }
  //        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
  // 밀리초 * 1000 + 3자리 난수 → 충돌 확률 1/1000으로 감소
]), [setTx]);
```

또는 더 강력하게:

```javascript
const genId = () => Date.now() * 10000 + (Math.random() * 10000 | 0);
const addTx = useCallback(t => setTx(ts => [...ts, { ...t, id: genId() }]), [setTx]);
```

---

### B5 🟠 DELETE 이벤트 미구독 → `clearAllTransactions` 파트너 미전파

#### 원인 분석

`supabase.js:123-141` 구독:

```javascript
// supabase.js:123-141 — 현재 코드 (버그: DELETE 없음)
subscribe(hid, onUpdate) {
  return supabase
    .channel(`realtime:household:${hid}`)
    .on('postgres_changes', { event: 'UPDATE', ... }, handler)
    .on('postgres_changes', { event: 'INSERT', ... }, handler)
    // ← DELETE 이벤트 없음!
    .subscribe();
}
```

`clearAllTransactions`가 DB에서 `tx_%` rows를 DELETE해도 파트너 기기에 전파 안 됨.

#### 수정 코드

```javascript
// supabase.js — subscribe에 DELETE 이벤트 추가
subscribe(hid, onUpdate) {
  return supabase
    .channel(`realtime:household:${hid}`)
    .on(
      'postgres_changes',
      { event: 'UPDATE', schema: 'public', table: 'household_data', filter: `id=eq.${hid}` },
      (payload) => onUpdate(payload.new.key, payload.new.value)
    )
    .on(
      'postgres_changes',
      { event: 'INSERT', schema: 'public', table: 'household_data', filter: `id=eq.${hid}` },
      (payload) => onUpdate(payload.new.key, payload.new.value)
    )
    .on(
      'postgres_changes',
      { event: 'DELETE', schema: 'public', table: 'household_data', filter: `id=eq.${hid}` },
      (payload) => {
        // 삭제된 키에 대해 빈 배열/null로 처리
        const key = payload.old?.key;
        if (key) onUpdate(key, key.startsWith('tx_') ? [] : null);
      }
    )
    .subscribe();
}
```

`App.jsx updateSharedState`에도 null 처리 추가:

```javascript
// App.jsx:74-104 — updateSharedState에 null/빈배열 처리 추가
const updateSharedState = useCallback((key, value) => {
  if (key.startsWith('tx_')) {
    const year = Number(key.slice(3));
    if (!isNaN(year) && loadedTxYears.current.has(year)) {
      setTxRaw(prev => {
        const filtered = prev.filter(t => {
          const [ty] = t.date.split('-').map(Number);
          return ty !== year;
        });
        // value가 null이면 해당 연도 tx 전부 제거
        return value ? [...filtered, ...value] : filtered;
      });
    }
    setLastSync(new Date());
    return;
  }
  // ... 기존 switch 유지
}, []);
```

---

### B6 🟡 `setShared` stale closure — `addToast` 의존성 누락

#### 원인 분석

`App.jsx:242-293`:

```javascript
// App.jsx:242-293 — useCallback deps 누락
const setShared = useCallback(async (key, value, rawSetter) => {
  // ...
  addToast(`${key} 저장 중 오류가 발생했습니다.`);  // line 261
  // ...
}, [householdId]);  // ← addToast 누락!
```

`addToast`가 렌더마다 재생성되는 경우, stale 버전이 호출될 수 있음.

#### 수정 코드

```javascript
// App.jsx:293 — deps 수정
}, [householdId, addToast]);
```

`Toast.jsx` 내 `addToast`가 `useCallback`으로 감싸져 안정적으로 반환되는지 확인 필요.  
만약 불안정하면 `useRef`로 최신 참조 유지:

```javascript
// App.jsx 최상단
const addToastRef = useRef(addToast);
useEffect(() => { addToastRef.current = addToast; }, [addToast]);

// setShared 내부에서
addToastRef.current(`${key} 저장 중 오류가 발생했습니다.`);
```

---

### B7 🟡 tx 1건 추가 시 전체 연도 저장 (성능)

#### 원인 분석

`App.jsx:321-324`:

```javascript
// App.jsx:321-324 — 현재 코드 (성능 낭비)
const allLoadedYears = Array.from(loadedTxYears.current);
await Promise.all(
  allLoadedYears.map(y => db.saveTx(householdId, y, byYear[y] ?? []))
  //              ^^^^  2020~2026년 모두 로드된 경우 7번 저장!
);
```

예: 2026년 tx 1건 추가 시 2020~2026 전 연도 저장 → 6번 불필요한 Supabase 호출.

#### 수정 코드

```javascript
// App.jsx:321-324 — 변경된 연도만 저장
const changedYears = Object.keys(byYear).map(Number);
// 기존 tx에서 없어진 연도도 처리 (삭제 케이스)
const allLoadedYears = Array.from(loadedTxYears.current);
const yearsToSave = new Set([...changedYears, ...allLoadedYears.filter(y => !byYear[y])]);

await Promise.all(
  Array.from(yearsToSave).map(y => db.saveTx(householdId, y, byYear[y] ?? []))
);
```

더 간단하게:

```javascript
// App.jsx — 변경된 연도와 빈 연도만
const yearsWithChanges = Object.keys(byYear).map(Number);
const emptyYears = Array.from(loadedTxYears.current).filter(y => !byYear[y]);
await Promise.all(
  [...yearsWithChanges, ...emptyYears]
    .map(y => db.saveTx(householdId, y, byYear[y] ?? []))
);
```

---

### B8 🟡 HomeView 검색 — 전체 tx 순회 (성능)

#### 원인 분석

`HomeView.jsx:356`:

```javascript
// HomeView.jsx:356 — 현재 코드 (성능 낭비)
const filtered = tx.filter(t => {
  if(!searchTerm) return true;
  // tx에는 2020~2026년 모두 포함될 수 있음!
  const label = CAT[t.cat]?.label || "";
  return (t.memo && t.memo.includes(searchTerm)) || label.includes(searchTerm);
}).sort((a,b)=>b.id-a.id);
```

#### 수정 코드

```javascript
// HomeView.jsx:355 — 현재 월 기준으로 제한 후 검색
const filtered = useMemo(() => {
  const base = searchTerm ? tx : thisMonthTx;  // 검색 없을 때는 이미 필터된 thisMonthTx 사용
  return base.filter(t => {
    if (!searchTerm) return true;
    const label = CAT[t.cat]?.label || "";
    return (t.memo && t.memo.includes(searchTerm)) || label.includes(searchTerm);
  }).sort((a,b) => b.id - a.id);
}, [tx, thisMonthTx, searchTerm]);
```

---

### B9 🟢 모듈 로드 시점 고정 날짜 상수

#### 원인 분석

`constants/index.js:48-52`:

```javascript
// constants/index.js:48-52 — 현재 코드 (잠재적 버그)
export const NOW   = new Date();
export const DAYS  = new Date(NOW.getFullYear(),NOW.getMonth()+1,0).getDate();
export const DAY   = NOW.getDate();
export const MONTH = NOW.getMonth()+1;
export const YEAR  = NOW.getFullYear();
// ↑ 앱이 자정 전에 열리면 영구적으로 어제 날짜
```

#### 수정 코드

주석 강화 및 사용 제한:

```javascript
// constants/index.js:48-52 — 경고 주석 추가
/**
 * @deprecated 컴포넌트에서 직접 사용하지 마세요.
 * 자정 이후 갱신되지 않아 날짜가 고정됩니다.
 * 대신 getYear(), getMonth(), getDay()를 사용하세요.
 */
export const NOW   = new Date();
export const DAYS  = new Date(NOW.getFullYear(),NOW.getMonth()+1,0).getDate();
export const DAY   = NOW.getDate();
export const MONTH = NOW.getMonth()+1;
export const YEAR  = NOW.getFullYear();
```

사용처를 함수형으로 교체:

```javascript
// 잘못된 사용 (찾아서 교체)
import { YEAR, MONTH, DAY } from "../constants";

// 올바른 사용
import { getYear, getMonth, getDay } from "../constants";
const YEAR  = getYear();
const MONTH = getMonth();
const DAY   = getDay();
```

---

### B10 🟢 Realtime 채널 누적 가능성

#### 원인 분석

`App.jsx:208-215`:

```javascript
// App.jsx:208-215 — 현재 코드
useEffect(() => {
  if (!setupDone || !householdId) return;
  const subscription = db.subscribe(householdId, (key, value) => {
    updateSharedState(key, value);
  });
  return () => { subscription.unsubscribe(); };
}, [setupDone, householdId, updateSharedState]);
```

`updateSharedState`가 의존성에 있어, `updateSharedState` 재생성 시 기존 구독을 `unsubscribe`하고 새로 구독.  
`updateSharedState`는 `useCallback([], [])` (빈 의존성)으로 안정적이므로 현재는 문제 없음.  
단, 향후 의존성 추가 시 중복 구독 위험 → 방어 코드 추가 권장.

```javascript
// supabase.js — 채널 중복 방지
subscribe(hid, onUpdate) {
  const channelName = `realtime:household:${hid}`;
  // 기존 채널이 있으면 제거
  const existing = supabase.getChannels().find(c => c.topic === channelName);
  if (existing) supabase.removeChannel(existing);
  
  return supabase
    .channel(channelName)
    .on(/* ... */)
    .subscribe();
}
```

---

## 3. 수정 우선순위 및 실행 계획

### Phase 1 — 즉시 수정 (서비스 불가 상태 해제) 🔴

1. ✅ **B0 Gemini API 404** → `api/ocr.js`, `api/budget-ai.js` → `v1beta/models/gemini-2.0-flash-lite` 적용 완료

2. ✅ **B1 bulk scan race condition** → `App.jsx:addTx` functional update + `addTxBatch` + CardScanSheet `onSaveAll` 완료

3. ✅ **B2 오프라인 tx 큐 미등록** → txSave useEffect 내 `navigator.onLine` 체크 + `enqueueOffline` 분기 완료

### Phase 2 — 이번 주 내 수정 (스키마 정합성) 🟠

4. ✅ **B3 고정비 name→label** → BudgetView.jsx / FixedView.jsx 양쪽 `name`→`label` 완료

5. ✅ **B4 ID 충돌** → `addTx` id = `Date.now() * 1000 + 난수` 완료

6. ✅ **B5 DELETE realtime** → `supabase.js` subscribe에 DELETE 핸들러 추가 완료

### Phase 3 — 성능 & 안정성 🟡

7. ✅ **B6 addToast deps** → `setShared` useCallback deps에 `addToast` 추가 완료

8. ✅ **B7 tx 저장 최적화** → `loadedTxYears`에 있는 연도만 저장 완료

9. ✅ **B8 검색 성능** → `filteredTx` useMemo + 검색어 없을 때 thisMonthTx만 순회 완료

10. ✅ **B9 날짜 상수** → `@deprecated` 주석 추가 완료

11. ✅ **B10 채널 누적** → `_channels` Map으로 채널 중복 방지 + `unsubscribe` 추가 완료

12. ✅ **ErrorBoundary** → `src/ErrorBoundary.jsx` 생성, `main.jsx` 래핑 완료

13. ✅ **TypeScript 에러 제로** → `tsconfig.json` checkJs, 전체 `error TS` 0건 확인

---

## 4. 테스트 시나리오

### B1 검증 (bulk scan)
```
1. 홈화면 → 📸 AI 스캔 터치
2. 카드내역 스크린샷 업로드 (5건 이상 포함)
3. 전체 선택 후 "5건 한꺼번에 저장" 터치
4. 홈화면 → 최근 내역에서 5건 모두 표시 확인
5. Supabase Dashboard에서 tx_YYYY 값에 5건 저장 확인
```

### B2 검증 (오프라인 tx)
```
1. 기기 비행기 모드 전환
2. 가계부 입력 (내역 추가)
3. 동기화 상태 "서버 연결 오류" 확인
4. 비행기 모드 해제
5. "오프라인 내역 1건 동기화 완료" 토스트 확인
6. 파트너 기기에서 해당 내역 확인
```

### B3 검증 (고정비)
```
1. 예산 탭 → 고정비/할부 탭
2. 고정비 추가 (이름, 금액, 날짜 입력)
3. 저장 후 목록에 정상 표시 확인
4. Supabase에서 fixed 배열의 각 항목에 label 필드 존재 확인
```

### B5 검증 (DELETE 전파)
```
1. 기기 A, B 동시 앱 오픈
2. 기기 A에서 설정 → 지출 내역 초기화
3. 기기 B에서 홈화면 자동 업데이트 (내역 0건) 확인 (재로드 없이)
```

---

## 5. 아키텍처 개선 제안 (중장기)

### 5-1. `setTx`를 `useReducer` 패턴으로 리팩터

현재 `setTx`는 저장 로직이 너무 많이 포함되어 있음. React 상태 관리와 비동기 저장 분리 권장:

```javascript
// 제안: txReducer 분리
const txReducer = (state, action) => {
  switch(action.type) {
    case 'ADD': return [...state, action.payload];
    case 'ADD_BATCH': return [...state, ...action.payload];
    case 'DELETE': return state.filter(t => t.id !== action.id);
    case 'EDIT': return state.map(t => t.id === action.id ? {...t, ...action.updates} : t);
    case 'SET_YEAR': return [...state.filter(t => !t.date?.startsWith(String(action.year))), ...action.data];
    default: return state;
  }
};
const [tx, dispatchTx] = useReducer(txReducer, EMPTY_TX);
```

### 5-2. Supabase 저장을 `useEffect` 기반으로 분리

현재: 상태 변경 함수 내에서 직접 저장  
제안: 상태 변경 → effect 감지 → 저장 (관심사 분리)

```javascript
// 저장 전용 effect
useEffect(() => {
  if (!householdId || !setupDone) return;
  const debounce = setTimeout(() => {
    // tx 저장 로직
  }, 300);
  return () => clearTimeout(debounce);
}, [tx, householdId, setupDone]);
```

### 5-3. Error Boundary 추가 (앱 크래시 방지)

```javascript
// src/ErrorBoundary.jsx
class ErrorBoundary extends React.Component {
  state = { hasError: false, error: null };
  static getDerivedStateFromError(error) { return { hasError: true, error }; }
  componentDidCatch(error, info) { console.error('App Error:', error, info); }
  render() {
    if (this.state.hasError) return (
      <div style={{ padding: 20, textAlign: 'center' }}>
        <div style={{ fontSize: 32, marginBottom: 12 }}>⚠️</div>
        <div style={{ fontSize: 14, marginBottom: 16 }}>앱에 오류가 발생했습니다</div>
        <button onClick={() => window.location.reload()}>새로고침</button>
      </div>
    );
    return this.props.children;
  }
}

// main.jsx에서 감싸기
<ErrorBoundary>
  <App />
</ErrorBoundary>
```

---

## 6. 변경 파일 요약표

| 파일 | 변경 내용 | 버그 |
|------|-----------|------|
| `api/ocr.js` | Gemini URL `v1beta/gemini-2.0-flash-lite`로 수정 | **B0** |
| `api/budget-ai.js` | 동일하게 URL 수정 | **B0** |
| `api/_gemini.js` (신규 선택) | 공통 모델명 상수 파일 | B0 |
| `src/App.jsx` | `addTx` functional update, `setTx.saveAll` 오프라인 분기, `setShared` deps 수정, tx 저장 최적화 | B1, B2, B4, B6, B7 |
| `src/components/CardScanSheet.jsx` | `handleSaveAll` 배치 저장 방식 전환 | B1 |
| `src/views/BudgetView.jsx` | `newF` state `name` → `label` 통일 | B3 |
| `src/utils/supabase.js` | `subscribe` DELETE 이벤트 추가, 채널 중복 방지 | B5, B10 |
| `src/utils/validate.js` | `fixed` 스키마에 `label` 필드 추가 | B3 |
| `src/views/HomeView.jsx` | 검색 `useMemo` + thisMonthTx 범위 제한 | B8 |
| `src/constants/index.js` | deprecated 주석 추가 | B9 |
| `src/main.jsx` | ErrorBoundary 래핑 추가 | 신규 |

---

*이 계획서는 2026-04-05 기준 소스 코드를 직접 분석하여 작성하였습니다.*
