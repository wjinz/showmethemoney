# 개선 계획서 — 2026-04-27 02:03 KST (rev.3 — claude 검증 + 보강)

> 기준: PROJECT_HISTORY.md 17단계까지 + 실제 소스코드 전수 재검증 + wody 추가 요청 + Antigravity 의견 + claude 보강
> 대상 브랜치: 현재 main (https://showmethemoney-eta.vercel.app)
> rev.3 변경 (claude):
> - 모든 항목을 src/ 실제 코드와 1:1 대조 검증 — 17단계 산출물 기준 정확성 확인
> - P0~P7 각 항목 root cause 재확인 후 [claude] 주석으로 누락/위험요소 명시
> - 신규 보강 항목 5건 추가: P0-5(IDB photos 데이터 소실), P1-5(handleDiarySave 부분실패), P2-5(타이머 누수), P3-5(다이어리 export), P8(테스트 자동화)
> - 우선순위표·구현 순서 재정렬

---

## 검증 결과 요약 (claude)

| 항목 | 계획서 주장 | 실제 코드 검증 | 검증 결과 |
|------|-------------|----------------|----------|
| P0-1 | DashboardView mergedTx 이중합산 | `[...diaries, ...txAsDiaries]` 합산 후 type='expense'만 필터 → diary expense + tx 중복 | 사실 (DashboardView.jsx:31~46) |
| P0-2 | SOS 채널 unsubscribe 누락 | `subscribeSos`는 `_channels`에 등록, App.jsx:446 cleanup 없음 | 사실 |
| P0-3 | sw.js diary 타입 미처리 | sw.js 87/104줄에 kv/tx만 분기, diary 분기 없음 | 사실 |
| P0-4 | InputSheet 날짜 고정 | InputSheet.jsx:7 `TODAY = new Date().toISOString()...`, handleSave에 `date: TODAY` 하드코딩 | 사실 |
| P1-1 | HistoryView 날짜 오프셋 이중오류 | `getTime() - getTimezoneOffset()*60000` + 렌더링 시 `+` 보정 → 이중 보정 | 사실 |
| P1-2 | editDiary가 tx 미동기화 | App.jsx:665 editDiary는 setDiaries만 호출, tx 무관 | 사실 |
| P1-3 | 자정 날짜 고정 | DiaryView.jsx:6, DashboardView.jsx:6, InputSheet.jsx:7 모두 모듈 최상위 const | 사실 |
| P1-4 | photos JSONB 폭발 | image.js 압축 적용 중이나 base64 JSONB 저장은 그대로 | 사실 |
| P2-2 | validate 차단 미구현 | validate.js:55 console.warn만, strict 옵션 없음 | 사실 |
| P2-3 | household_data RLS 부재 | supabase_setup.sql에서 transactions/sos만 RLS, household_data 없음 | 사실 |
| P2-4 | NOW/DAYS 등 deprecated | constants/index.js:96~100에 export 남아있음 | 사실 |
| P5 | Settlement 급여 필드 부재 | currentCash 라벨 = "지금 가진 현금"/"보유 현금" → 의미 모호 | 사실 |
| P6 | FixedItem who 필드 부재 | constants/index.js:3 typedef에 who 없음 | 사실 |
| P7-2 | AI allowanceTotal 누락 | BudgetView.jsx:380 body에 allowanceTotal 미포함, runLocalAI 시그니처에 없음 | 사실 |

> 결론: rev.2 계획서의 모든 분석은 실제 코드와 일치한다. 이번 rev.3에서는 누락 우려가 있는 5건을 보강한다.

---

## P0 — 즉시 수정 (프로덕션 데이터 오염 / 메모리 누수)

### P0-1. DashboardView 이중 지출 카운팅

**파일:** `src/views/DashboardView.jsx`

**원인:**
`handleDiarySave` (App.jsx)는 diary type='expense' 저장 시 `addTxBatch`로 tx에도 동시 기록한다 (Antigravity-4).
그런데 DashboardView는 `tx`를 diary 형태로 변환한 `txAsDiaries`와 `diaries` 배열을 그대로 합산한다.
결과적으로 동일 지출이 diaries 배열과 tx 배열 양쪽에 존재하여 합산 금액이 2배가 된다.

**현재 코드 (오류):**
```js
// DashboardView.jsx:30~46
const mergedTx = useMemo(() => {
  const txAsDiaries = tx.map(t => ({ id: t.id, type: 'expense', /* ... */ }));
  return [...diaries, ...txAsDiaries];
}, [tx, diaries]);

const monthRecords = mergedTx.filter(d =>
  d.date.startsWith(currentMonth) && d.type === 'expense'
);
const hSpent = monthRecords
  .filter(d => d.who === 'husband')
  .reduce((s, d) => s + (d.totalSpent || 0), 0);
```

**수정안:**
DiaryView.jsx가 이미 올바른 방식을 사용한다 — `tx` 배열만으로 월 지출을 계산.
DashboardView도 동일하게 `tx`를 단일 소스로 사용하고, diary는 감성/이모지 통계에만 활용한다.

```js
const monthTx = useMemo(() =>
  tx.filter(t => typeof t.date === 'string' && t.date.startsWith(currentMonth)),
  [tx, currentMonth]
);
const hSpent = monthTx.filter(t => t.who === 'husband').reduce((s, t) => s + (t.amount || 0), 0);
const wSpent = monthTx.filter(t => t.who === 'wife').reduce((s, t) => s + (t.amount || 0), 0);
const totalSpent = hSpent + wSpent;

const catStats = useMemo(() => monthTx.reduce((acc, t) => {
  if (!t.cat) return acc;
  acc[t.cat] = (acc[t.cat] || 0) + (t.amount || 0);
  return acc;
}, {}), [monthTx]);

const monthDiaryOnly = useMemo(() => diaries.filter(d =>
  d.date.startsWith(currentMonth) && d.type === 'diary'
), [diaries, currentMonth]);
```

> **[Antigravity]** DashboardView는 리렌더링이 잦은 위젯 구조이므로, 위 계산 로직은 반드시 `useMemo`를 사용하여 tx 배열이 바뀔 때만 실행되도록 보장해야 합니다. 또한, `ReportView.jsx` 등 다른 통계 뷰에서도 이와 유사한 이중 합산 로직이 있는지 전수 조사가 필요합니다.

> **[claude]** 회귀 방지 가드 필수. Vitest 도입(P8) 전까지는 dev console에서 시각 검증 후 push. 또한 ReportView.jsx의 `DAY > 0 ? Math.round(curTotalAll / DAY * DAYS)`(line 74) 합산도 tx 단일소스 기준이어야 한다. 동일 패턴 점검 대상.

---

### P0-2. SOS 채널 unsubscribe 누락 (메모리 누수)

**파일:** `src/App.jsx`, `src/utils/supabase.js`

**원인:**
`db.subscribeSos`는 `sos:${hid}` 채널을 `_channels` 맵에 등록하지만,
useEffect cleanup에서는 `db.unsubscribe(householdId)`만 호출한다.
이 함수는 `realtime:household:${hid}` 채널만 제거하므로 SOS 채널이 계속 살아있다.

**수정안:**
```js
// src/utils/supabase.js
unsubscribeSos(hid) {
  const channelName = `sos:${hid}`;
  const ch = this._channels.get(channelName);
  if (ch) {
    supabase.removeChannel(ch);
    this._channels.delete(channelName);
  }
},

// App.jsx
useEffect(() => {
  if (!setupDone || !householdId) return;
  db.subscribeSos(householdId, (req) => { /* ... */ });
  return () => { db.unsubscribeSos(householdId); };
}, [setupDone, householdId, myRole, addToast]);
```

> **[Antigravity]** 향후 구독 채널이 늘어날 경우를 대비해, db 객체 내에 모든 활성 채널을 한 번에 정리하는 `unsubscribeAll(hid)` 메서드 도입을 강력히 권장합니다.

> **[claude]** `unsubscribeAll(hid)` 구현 시 `_channels` Map을 직접 순회하기보다 prefix(`realtime:household:`, `sos:`) 기준으로 매칭하는 방식이 안전. `subscribeSos`가 이미 등록된 채널을 재사용하도록 가드(supabase.js:325)되어 있는지 검증 완료. cleanup만 추가하면 누수 0건.

---

### P0-3. sw.js Background Sync diary 타입 미처리

**파일:** `public/sw.js`

**원인:**
`offlineIDB.js`는 `type: 'diary'` 항목을 IDB에 저장할 수 있도록 설계되어 있다.
그러나 `sw.js`의 Background Sync 핸들러는 `kv`와 `tx` 타입만 처리한다.

```js
// public/sw.js:87,104
for (const item of items) {
  if (item.type === 'kv') { /* ... */ }
  else if (item.type === 'tx') { /* ... */ }
  // item.type === 'diary' 미처리 → IDB에 영구 잔류
}
```

> **[Antigravity]** SW에서 다이어리를 무시하는 대신, 최소한 'diary-sync-pending' 알림을 띄워 사용자가 앱을 닫기 전 동기화 필요성을 인지하게 하는 로직을 추천합니다. 사진 데이터(Base64)가 포함된 경우 페이로드 크기가 커서 동기화 실패율이 높을 수 있으므로 오프라인 상태에서의 사진 첨부 제한 혹은 경고 문구 추가도 고려해야 합니다.

> **[claude]** rev.2 수정안의 `succeeded.push(item.idbId)`로 IDB에서 무조건 제거하는 것은 위험. App이 닫힌 상태에서 SW만 활성화되어 동기화하는 시나리오에서는 `flushIdbDiaries`가 호출되지 못해 데이터 영구 소실 가능성. 권장 방식:
> ```js
> else if (item.type === 'diary') {
>   // App 활성화 시 flushIdbDiaries가 처리. SW에서는 PASS (큐 유지)
>   console.log('[SW] diary 항목은 App에 위임 (큐 유지)');
> }
> ```

---

### P0-4. [wody 요청] 입력 화면 날짜 선택 기능 부재

**파일:** `src/components/InputSheet.jsx`

**현상:**
지출 및 다이어리 입력 시 날짜가 모듈 로드 시점의 `TODAY`로 고정되어 있어 과거 내역을 소급 입력할 수 없다.

**수정안:**
```js
const [entryDate, setEntryDate] = useState(() => {
  const d = new Date();
  return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
});

<input
  type="date"
  value={entryDate}
  max={(() => { const d=new Date(); return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`; })()}
  onChange={e => setEntryDate(e.target.value)}
/>

onSave({ date: entryDate, /* ... */ });
```

> **[claude]** 미래 날짜 입력 차단(`max` 속성)은 위 코드에 포함되어 있으나, 추가 제약:
> 1. 편집(DetailSheet)에서도 동일 패턴 적용 필수 — 현재 DetailSheet는 cat/payMethod/cardId만 편집 가능. 날짜 변경 미지원이므로 P1-2 editDiary 보강과 동기 처리.
> 2. 너무 먼 과거 입력 방지 — 1년 이전 입력은 confirm 한 번 띄우는 게 안전.
> 3. time 필드도 함께 수정 — 현재 `handleSave`에서 `new Date().getHours()`로 현재 시간을 사용(InputSheet.jsx:101). 소급 입력 시 time을 12:00 등 합리적 기본값으로 두거나 추가 입력 받아야 한다.

---

### [claude 보강] P0-5. 오프라인 IDB 큐 photos 데이터 소실 위험

**파일:** `src/utils/offlineIDB.js`, `src/App.jsx`

**현상 (claude 신규 발견):**
오프라인 중 사진 포함 다이어리를 IDB에 큐잉하면 base64 photos가 그대로 들어간다.
사용자가 오프라인에서 5장 이상 다이어리 작성 후 앱을 강제 종료하면:
- App.jsx의 `flushIdbDiaries`는 앱 재시작 + 온라인 복구 시에만 동작
- SW의 sync 이벤트는 위 P0-3 수정안으로 PASS 처리

→ 큐가 무한 누적될 가능성. quota 초과 시 IDB write 실패 → 사용자 데이터 즉시 소실.

**수정안:**
```js
// src/utils/offlineIDB.js
export async function idbEnqueueDiary(item) {
  if (navigator.storage && navigator.storage.estimate) {
    const { usage = 0, quota = 1 } = await navigator.storage.estimate();
    if (usage / quota > 0.85) {
      console.warn('[IDB] quota 85% 초과 - 큐잉 거절');
      throw new Error('STORAGE_QUOTA_EXCEEDED');
    }
  }
}

// App.jsx — handleDiarySave에서 quota 에러 처리
try {
  await enqueueWithSync({ type: 'diary', householdId, payload: diaryItem });
} catch (e) {
  if (e.message === 'STORAGE_QUOTA_EXCEEDED') {
    addToast('저장 공간이 부족합니다. 온라인 상태에서 다시 시도해주세요.', 'error');
  }
}
```

---

## P1 — 빠른 수정 (데이터 오류 / UX 버그)

### P1-1. HistoryView 날짜 오프셋 이중 오류

**파일:** `src/views/HistoryView.jsx`

**원인:**
`selectedDate.getTime() - selectedDate.getTimezoneOffset() * 60000` 후 다시 렌더링에서 `+` 보정 → 이중 보정.

**수정안:**
```js
function toLocalDateStr(date) {
  const y = date.getFullYear();
  const m = String(date.getMonth() + 1).padStart(2, '0');
  const d = String(date.getDate()).padStart(2, '0');
  return `${y}-${m}-${d}`;
}

const selStr = toLocalDateStr(selectedDate);
const d = new Date(date + 'T00:00:00');
const wd = ['일','월','화','수','목','금','토'][d.getDay()];
```

> **[Antigravity]** 이러한 날짜 관련 버그는 앱 전반(EntryView, CalendarView 등)에서 발생할 위험이 큽니다. 날짜 처리를 전담하는 전역 유틸리티(`src/utils/dateUtils.js`)를 만들어 표준화된 로컬 시간 변환 로직을 사용해야 합니다.

> **[claude]** helpers.js에 `today_str()`, `toDateStr(d)`가 이미 존재(helpers.js:15,33). 새 dateUtils.js 신설보다 helpers.js에 통합이 적절. 추가할 함수:
> ```js
> export function parseLocalDate(yyyymmdd) {
>   return new Date(yyyymmdd + 'T00:00:00');
> }
> ```
> 그리고 EntryView/CalendarView/ReportView에서 `toISOString().split('T')[0]` 패턴을 grep으로 전수 점검 후 일괄 교체.

---

### P1-2. editDiary 시 연결된 tx 미동기화

**파일:** `src/App.jsx`, `src/components/DetailSheet.jsx`

**수정안:**
```js
const editDiaryWithTx = useCallback((updated) => {
  editDiary(updated);
  if (updated.type !== 'expense') return;
  setTx(prev => prev.map(t =>
    t.source_id === updated.id
      ? { ...t, amount: updated.totalSpent || t.amount, memo: updated.content || t.memo, date: updated.date || t.date }
      : t
  ));
}, [editDiary, setTx]);
```

> **[claude]** 부분 실패 시나리오 누락 위험. editDiary와 setTx가 별도 useEffect 디바운스 저장(App.jsx:344~)을 타기 때문에, 한쪽만 저장되고 한쪽이 실패하면 데이터 불일치가 영구화된다. 다음 방어 코드 추가:
> ```js
> const editDiaryWithTx = useCallback((updated) => {
>   const prevDiary = diaries.find(d => d.id === updated.id);
>   try {
>     editDiary(updated);
>     if (updated.type === 'expense') {
>       setTx(prev => prev.map(t =>
>         t.source_id === updated.id
>           ? { ...t, amount: updated.totalSpent || t.amount, memo: updated.content || t.memo, date: updated.date || t.date }
>           : t
>       ));
>     }
>   } catch (e) {
>     if (prevDiary) editDiary(prevDiary);
>     addToast('편집 실패: 데이터를 복원합니다', 'error');
>   }
> }, [editDiary, setTx, diaries, addToast]);
> ```
> 또한 삭제 시도 동일 — `deleteDiary` → 연결된 tx도 함께 삭제하는 `deleteDiaryWithTx` 필요. (현재는 tx 고아 데이터로 잔존)

---

### P1-3. 자정 날짜 고정 버그

**파일:** `src/views/DiaryView.jsx`, `src/views/DashboardView.jsx`, `src/components/InputSheet.jsx`

**수정안:**
```js
export function DiaryView({ onOpenSheet }) {
  const today = useMemo(() => {
    const d = new Date();
    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
  }, []);
}
```

> **[claude]** `useMemo(() => ..., [])`는 마운트 시점에만 계산되므로 하루를 가로지르는 세션(예: 23:59 → 00:01)에는 여전히 어제 날짜가 남는다. 권장: 페이지 visibility 이벤트 리스너로 `visibilitychange` 시 `today` 재계산.
> ```js
> useEffect(() => {
>   const onFocus = () => setRenderKey(k => k + 1);
>   document.addEventListener('visibilitychange', onFocus);
>   return () => document.removeEventListener('visibilitychange', onFocus);
> }, []);
> ```

---

### P1-4. diaries JSONB blob 사진 용량 폭발 위험

**파일:** `src/components/InputSheet.jsx`, `src/utils/image.js`

**수정안 1단계 (즉시):**
```js
const dataUrl = await compressImage(file, {
  maxWidth: 600,
  quality: 0.65,
  format: 'image/webp',
});
setPhotos(p => [...p, dataUrl].slice(0, 2));
```

**수정안 2단계 (중기 — Supabase Storage 분리):**
```js
async function uploadPhoto(file, householdId) {
  const compressed = await compressImage(file, { maxWidth: 600, quality: 0.65 });
  const blob = await fetch(compressed).then(r => r.blob());
  const path = `${householdId}/${Date.now()}.webp`;
  const { data, error } = await supabase.storage
    .from('diary-photos').upload(path, blob);
  if (error) throw error;
  return supabase.storage.from('diary-photos').getPublicUrl(data.path).data.publicUrl;
}
```

> **[Antigravity]** 2단계(Storage 분리) 전까지는 '설정' 메뉴에 '다이어리 캐시/사진 정리' 기능을 추가하여 6개월 이상 된 데이터의 사진 base64를 선택적으로 제거할 수 있는 유틸리티를 제공해야 합니다. 또한 Storage 도입 시, 기존의 Base64 데이터를 자동으로 Storage로 옮기고 URL로 치환해주는 '데이터 마이그레이션 스크립트' 설계가 선행되어야 데이터 폭증 문제를 근본적으로 해결할 수 있습니다.

> **[claude]** Storage 분리 시 RLS 정책 필수. anon key로 자유 업로드/조회되면 보안 사고 직결. 정책 예시:
> ```sql
> CREATE POLICY "household_photos_read" ON storage.objects FOR SELECT
>   USING (bucket_id = 'diary-photos' AND (storage.foldername(name))[1] = current_setting('app.household_id', true));
> CREATE POLICY "household_photos_write" ON storage.objects FOR INSERT
>   WITH CHECK (bucket_id = 'diary-photos' AND (storage.foldername(name))[1] = current_setting('app.household_id', true));
> ```
> 또한 1단계의 압축률 강화(`quality: 0.5`까지) 시 OCR 영수증 다이어리 첨부 가독성이 떨어진다. 다이어리/지출 모드별로 `quality`를 분리하는 것을 검토.

---

### [claude 보강] P1-5. handleDiarySave 부분 실패 핸들링 부재

**파일:** `src/App.jsx:678~735`

**현상 (claude 신규 발견):**
`handleDiarySave`는 `setDiaries` → `addTxBatch` 순으로 호출되지만, 두 저장 경로가 다르다. 한쪽만 저장 성공/실패하면 다이어리 표시는 되는데 합계 계산은 0이거나 그 반대.

**수정안 (commit 패턴):**
1. tempId로 양쪽 모두 추가 (낙관적 UI)
2. 디바운스 저장 effect 결과를 watch → 둘 다 성공한 경우에만 status='committed' 업데이트
3. 실패한 항목은 'pending' 상태로 표시 + 재시도 버튼 노출

---

## P2 — 구조 개선 (성능 / 유지보수)

### P2-1. setter 함수의 과도한 deps로 인한 리렌더링

**파일:** `src/App.jsx`

**수정안:**
```js
const setFixed = useCallback(
  (v) => {
    setFixedRaw(prev => {
      const next = typeof v === 'function' ? v(prev) : v;
      return next;
    });
  },
  []
);
```

> **[claude]** setShared의 부수효과(validate + db.save 트리거)를 살리려면 useRef로 saveTrigger 콜백을 보관하는 패턴이 더 적절:
> ```js
> const saveTriggerRef = useRef();
> saveTriggerRef.current = (key, value) => { validate(key, value); /* save */ };
>
> const setFixed = useCallback((v) => {
>   setFixedRaw(prev => {
>     const next = typeof v === 'function' ? v(prev) : v;
>     saveTriggerRef.current?.('fixed', next);
>     return next;
>   });
> }, []);
> ```
> 이렇게 하면 setFixed의 referential identity가 마운트 후 평생 유지된다 → 자식 컴포넌트 React.memo 효율 극대화.

---

### P2-2. validate.js 저장 차단 로직 부재

**파일:** `src/utils/validate.js`

**수정안:**
```js
export function validate(key, value, opts = {}) {
  const checker = SCHEMAS[key];
  if (!checker) return true;
  const ok = checker(value);
  if (!ok) {
    console.warn(`[validate] '${key}' 스키마 불일치`, value);
    if (opts.strict) throw new Error(`[validate] '${key}' schema mismatch`);
  }
  return ok;
}
```

> **[claude]** strict 모드를 cards에만 적용하면 실수로 다른 키(`tx`, `fixed`)가 깨졌을 때 침묵한다. 차라리 production은 console.error + 사용자 토스트, dev는 throw 정책이 안전:
> ```js
> if (!ok) {
>   if (import.meta.env.DEV) throw new Error(...);
>   else console.error(...);
> }
> ```

---

### P2-3. household_data RLS 정책 미적용

**파일:** `supabase_setup.sql`, `src/utils/supabase.js`

**수정안:**
```sql
ALTER TABLE household_data ENABLE ROW LEVEL SECURITY;

CREATE POLICY "household_data_select" ON household_data
  FOR SELECT USING (
    id = current_setting('app.household_id', true)
    OR id = 'GLOBAL_SYSTEM'
  );

CREATE POLICY "household_data_write" ON household_data
  FOR ALL USING (id = current_setting('app.household_id', true))
  WITH CHECK (id = current_setting('app.household_id', true));
```

> **[claude]** 치명적 누락 추가 — 클라이언트 코드가 `current_setting('app.household_id')`를 어디에서도 SET 하지 않고 있다. supabase.js의 `db.subscribe`/`loadAll` 직전에 SET을 보내야 RLS가 동작한다:
> ```js
> async setHouseholdContext(hid) {
>   const { error } = await supabase.rpc('set_household_id', { hid });
>   if (error) console.error('[RLS] set context fail:', error);
> }
> ```
> ```sql
> CREATE OR REPLACE FUNCTION set_household_id(hid text) RETURNS void AS $$
>   SELECT set_config('app.household_id', hid, true);
> $$ LANGUAGE sql;
> ```
> 이걸 안 하면 RLS 켜는 순간 앱 전체가 빈 화면으로 죽는다. 단계적 배포 필수:
> 1. `set_household_id` RPC 먼저 배포 + 클라이언트 호출 위치 추가
> 2. 충분히 검증 후 RLS 정책 enable
> 3. SOS/transactions에도 동일한 set 패턴 정착

---

### P2-4. constants/index.js deprecated 변수 제거

**파일:** `src/constants/index.js`

```js
// 삭제 대상 (constants/index.js:96~100)
export const NOW   = new Date();
export const DAYS  = new Date(NOW.getFullYear(),NOW.getMonth()+1,0).getDate();
export const DAY   = NOW.getDate();
export const MONTH = NOW.getMonth()+1;
export const YEAR  = NOW.getFullYear();
```

> **[claude]** 이미 검증 완료 — HomeView/ReportView/WidgetView는 모두 `getYear(), getMonth(), getDay()` 함수를 import 받아 로컬 변수명으로 YEAR/MONTH/DAY를 사용 중. 따라서 constants의 export만 제거하면 충돌 없이 통과한다. 단, `import { NOW, DAYS, DAY, MONTH, YEAR } from '...'` 패턴 점검 후 제거:
> ```bash
> grep -rn "import.*\(NOW\|DAYS\|MONTH\|YEAR\)\b.*from.*constants" src/
> ```

---

### [claude 보강] P2-5. 타이머 누수 일제 점검

**파일:** `src/App.jsx`

**수정안:**
```js
const timersRef = useRef(new Set());
const safeSetTimeout = (fn, ms) => {
  const id = setTimeout(() => { timersRef.current.delete(id); fn(); }, ms);
  timersRef.current.add(id);
  return id;
};
useEffect(() => () => {
  timersRef.current.forEach(clearTimeout);
  timersRef.current.clear();
}, []);
```

---

## P3 — 미래 지향적 고도화 아이디어 (Antigravity + claude)

### P3-1. 다이어리 통합 검색 (Full-text Search)
**내용:** 다이어리 본문 및 메모가 늘어남에 따라 키워드 검색 기능이 필수적입니다.
**방법:** `DiaryView` 상단에 검색 바 추가, 로컬 `diaries` 배열에 대한 `filter` 검색 구현.

> **[claude]** 1,000개 이상 누적되면 단순 filter도 60fps에서 끊긴다. fuse.js(라이브러리 추가 비용)보다는 Web Worker + 인덱스 캐싱이 가볍다. 단계적: 우선 단순 filter, 다이어리 500개 돌파 시 worker 도입.

### P3-2. 실시간 편집 충돌 방지 (Optimistic Locking)
**내용:** 부부가 동시에 같은 다이어리/지출을 수정할 경우 마지막 저장값이 덮어씌워지는 문제.
**방법:** `updated_at` 타임스탬프를 비교하여 내가 수정하는 동안 파트너가 먼저 수정했다면 "새로운 버전이 있습니다" 안내 팝업 노출.

### P3-3. 지출 항목별 삭제 버튼 추가
**내용:** `InputSheet`에서 지출 항목(item)을 추가한 후 개별적으로 삭제할 수 있는 기능이 현재 부족합니다.
**방법:** 각 항목 우측에 휴지통 아이콘 추가 및 `handleRemoveItem` 로직 구현.

### P3-4. 동기화 상태 상세 인디케이터
**내용:** `SyncBar`의 '연결됨' 표시가 구체적으로 어떤 데이터가 업로드 중인지 알려주지 않음.
**방법:** 오프라인 큐의 개수를 표시 (예: "클라우드 연결됨 (3건 대기 중)") 하여 사용자 신뢰도 향상.

### [claude 신규] P3-5. 다이어리 PDF/이미지 내보내기 (가족 추억 백업)
**내용:** 월별/연별 다이어리를 한 페이지 이미지나 PDF로 내보내 인스타그램·카카오톡 공유 가능.
**방법:** `html2canvas` + `jspdf` 조합 또는 SVG 직렬화. 사진 5개 + 다이어리 본문 + 월간 지출 요약을 1장으로 합성.
**효과:** 가족 일기장의 정서적 가치 ↑, 앱 락인(retention) 효과 ↑.

---

## P4 — 시스템 안정성 및 편의성 강화 (장기, Antigravity + claude)

### P4-1. 전역 에러 바운더리 리포팅 연동
- **내용**: `ErrorBoundary.jsx`에서 런타임 에러 포착 시, `db.reportBug`를 호출하여 개발자에게 환경 정보와 스택 트레이스를 자동 전송.

> **[claude]** 무한 루프 방지 — ErrorBoundary 안에서 reportBug가 throw하면 재진입 발생. try/catch + 1회만 호출하는 hasReportedRef 가드 필수.

### P4-2. 데이터 전체 내보내기/가져오기 (Backup & Restore)
- **내용**: 모든 공유 데이터(household_data)와 트랜잭션을 JSON/CSV 포맷으로 로컬 다운로드 및 업로드 기능 제공.

### P4-3. 컴포넌트 단위 리팩토링 (SRP 준수)
- **내용**: 150라인이 넘는 대형 컴포넌트(`App.jsx`(1066줄), `BudgetView.jsx`(795줄) 등)를 작은 단위로 분할.

> **[claude]** 우선순위: App.jsx의 useEffect 9개 → 각각 custom hook으로 추출 (`useRealtimeSync`, `useOfflineQueue`, `useSosSubscription` 등). 이 단계만으로도 App.jsx가 ~600줄로 감소.

---

## [wody] P5 — 이번 달 정산하기 (Settlement) 기능 고도화

**파일:** `src/views/SettlementView.jsx`

**현재 상태:**
`currentCash` 라벨 = "지금 가진 현금"/"보유 현금" → 의미 모호. wody 의도는 *당월 급여에서 카드값과 고정비를 빼는 계산기*.

**수정안:**
```js
// SettlementView.jsx 라인 234 — LabeledInput 라벨 변경
<LabeledInput label="이번 달 실수령 급여 (부부 합산)" value={currentCash} onChange={setCurrentCash} />

// SummaryCard 내부 라인 213
<Row label="실수령 급여" value={`+${currentCash.toLocaleString()}원`} />
```

> **[claude]** 변수명(`currentCash`)을 그대로 두고 라벨만 바꾸는 것은 향후 코드 유지보수에 큰 혼란. 마이그레이션 전략:
> 1. 단기: settlements 데이터 구조에 `salary` 필드 신규 추가, `currentCash`는 `null`이면 `salary`로 fallback
> 2. 중기: 모든 사용처에서 `currentCash` → `salary` 일괄 치환
> 3. 장기: settlements 마이그레이션 스크립트로 기존 데이터 `currentCash` → `salary` 이동
> 또한 부부 개별 급여를 따로 입력받는 옵션도 고려 (husband/wife 토글). 이 경우 누가 더 많이 벌었는지 정산 결과에 표시되어 부부 간 가시성 ↑.

---

## [wody] P6 — 고정비/할부 탭 상세화 및 시각화

**파일:** `src/views/BudgetView.jsx` (FixedTab), `src/constants/index.js`

**수정안 1 — typedef 확장:**
```js
// @typedef {{ id: string|number, label: string, amount: number, cat?: string, day?: number, cardId?: string, who?: 'husband'|'wife' }} FixedItem
// @typedef {{ id: string|number, label: string, totalAmount: number, months: number, monthly: number, cardId?: string, date?: string, who?: 'husband'|'wife' }} InstallItem
```

**수정안 2 — 입력 폼에 입력자 필드 추가:**
```js
const [newF, setNewF] = useState({ label: '', amount: 0, cat: 'housing', day: '', type: 'f', who: 'husband' });
const [newI, setNewI] = useState({ label: '', total: 0, months: '', cardId: '', date: '', who: 'husband' });

<div style={{ display:'flex', gap:8 }}>
  {['husband','wife'].map(r => (
    <button key={r} onClick={() => setNewF(p => ({ ...p, who: r }))}>
      {r === 'husband' ? names.husband : names.wife}
    </button>
  ))}
</div>
```

**수정안 3 — 할부 종료일 계산:**
```js
function getInstallEndDate(item) {
  if (!item.date || !item.months) return null;
  const [y, m] = item.date.split('-').map(Number);
  const endDate = new Date(y, m - 1 + item.months, 1);
  return `${endDate.getFullYear()}-${String(endDate.getMonth()+1).padStart(2,'0')}`;
}
```

**수정안 4 — 할부 간트 차트:**
```js
function InstallGantt({ install }) {
  const now = new Date();
  const months = Array.from({ length: 12 }, (_, i) => {
    const d = new Date(now.getFullYear(), now.getMonth() + i - 2, 1);
    return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;
  });

  return (
    <div style={{ overflowX:'auto', marginTop:16 }}>
      <div style={{ display:'grid', gridTemplateColumns:`120px repeat(12, 40px)`, gap:2, minWidth:600 }}>
        <div />
        {months.map(m => <div key={m} style={{ fontSize:9, textAlign:'center' }}>{m.slice(5)}월</div>)}
        {install.map(item => {
          const start = item.date ? item.date.slice(0,7) : null;
          const endDate = start
            ? (() => { const [y,m] = start.split('-').map(Number); const d = new Date(y, m-1+item.months,1); return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`; })()
            : null;
          return (
            <React.Fragment key={item.id}>
              <div style={{ fontSize:12 }}>{item.label}</div>
              {months.map(m => {
                const active = start && endDate && m >= start && m < endDate;
                return <div key={m} style={{ height:20, borderRadius:4, background: active ? 'var(--primary)' : 'var(--cream3)', opacity: active ? 0.85 : 0.3 }} />;
              })}
            </React.Fragment>
          );
        })}
      </div>
    </div>
  );
}
```

> **[claude]** 보강:
> 1. 현재 월 강조: months 중 `now.getFullYear()-now.getMonth()` 일치 칸에 border-bottom 강조 추가하면 가독성 ↑.
> 2. 할부 종료 임박 알림: `endDate - now <= 1month`인 항목은 녹색 점 표시 — 사용자에게 "이번 달이면 끝" 정보 전달.
> 3. 누적 월별 합계: 그리드 맨 아래 row에 `해당 월 할부 합계`를 표시하면 정산 예측에 즉시 활용 가능.
> 4. who(husband/wife)별 색상 구분: husband=blue, wife=pink 등 필터링 시 카테고리별 시각화.

---

## [wody] P7 — 카드사 이용기간 로직 및 AI 예산 추천 로직 점검

### P7-1. 카드 이용기간 합산 로직 확인

```js
function getCardBillingTotal(card, tx) {
  const { cycleStart, cycleEnd } = getBillingPeriod(card);
  return tx
    .filter(t => t.cardId === String(card.id))
    .filter(t => {
      const d = new Date(t.date + 'T00:00:00');
      return d >= cycleStart && d <= cycleEnd;
    })
    .reduce((s, t) => s + t.amount, 0);
}
```

> **[claude]** 보강:
> 1. 할부 항목 처리: install 배열의 monthly도 카드 합산에 포함해야 한다 (`installItem.cardId === card.id`이고 현재 월이 결제 월에 해당).
> 2. 고정비 처리: fixed.cardId가 일치하는 항목도 동일하게 더해야 함.
> 3. 차이 표시: 사용자가 입력한 카드 청구액과 앱 기록값의 차이를 색깔로 강조 (>10% 차이면 빨간 경고).
> 4. 자동 채움 버튼: "앱 기록값으로 채우기" 버튼 클릭 시 `setCardBills`로 자동 입력.

### P7-2. AI 추천 예산 한도 초과 버그 — root cause 특정

**원인 (코드 분석으로 확인):**
```js
// BudgetTab 내부 (BudgetView.jsx:344) — allowanceTotal을 차감
const monthlyAvailRaw = Math.max(0,
  totalSalary - fixedTotal - installTotal - (salary.savingsTarget || 0) - allowanceTotal
);

// runAI 호출 (BudgetView.jsx:380) — allowanceTotal이 전달되지 않음
body: JSON.stringify({
  totalSalary, fixedTotal, installTotal,
  savingsTarget: salary.savingsTarget,
  catHistory, utilizationTarget: utilTarget
})

// runLocalAI 내부 (BudgetView.jsx:26)
const rawAvail = Math.max(0, totalSalary - fixedTotal - installTotal - savingsTarget);
// ← BudgetTab의 monthlyAvailRaw보다 allowanceTotal만큼 큼
```

**수정안:**
```js
const runAI = async () => {
  setAiLoading(true);
  const catHistory = buildCatHistory();
  try {
    const resp = await fetch('/api/budget-ai', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        totalSalary, fixedTotal, installTotal,
        savingsTarget: salary.savingsTarget,
        allowanceTotal,
        catHistory, utilizationTarget: utilTarget
      })
    });
  } catch {
    setAiResult(runLocalAI(
      totalSalary, fixedTotal, installTotal,
      salary.savingsTarget,
      catHistory, utilTarget,
      allowanceTotal
    ));
  }
};

const runLocalAI = (totalSalary, fixedTotal, installTotal, savingsTarget, catHistory, utilizationTarget = 100, allowanceTotal = 0) => {
  const rawAvail = Math.max(0, totalSalary - fixedTotal - installTotal - savingsTarget - allowanceTotal);
  const monthlyAvail = Math.round(rawAvail * Math.min(Math.max(utilizationTarget, 50), 100) / 100);

  const suggestedTotal = Object.values(suggested).reduce((s, v) => s + v, 0);
  if (suggestedTotal > monthlyAvail) {
    const scale = monthlyAvail / suggestedTotal;
    Object.keys(suggested).forEach(k => {
      suggested[k] = Math.round(suggested[k] * scale / 1000) * 1000;
    });
  }
};
```

> **api/budget-ai.js 동일 수정 필요**: 서버 함수에서도 `allowanceTotal`을 요청 body에서 읽어 계산에 포함.

> **[claude]** 보강:
> 1. 하위 호환: api/budget-ai.js에서 `allowanceTotal ?? 0`으로 기본값 처리. 구버전 클라이언트 충돌 방지.
> 2. 검증 케이스: 수정 후 다음 시나리오 테스트 — `allowanceTotal=500000, totalSalary=5000000, fixed=2000000, install=300000, savings=500000` → AI 추천 합계는 정확히 1,700,000원 이하여야 한다.
> 3. AI 응답 검증 게이트: API 응답이 한도 초과로 와도 클라이언트에서 정규화(scale)하여 무조건 monthlyAvail 이하로 맞추는 안전망 유지.

---

## [claude 신규] P8 — 테스트 자동화 인프라 도입

**배경:**
17단계까지 빠른 이터레이션으로 누적된 회귀 위험이 P0-1(이중합산), P7-2(AI 한도) 등 반복적으로 등장. 매번 수동 검증으로는 비용이 큼.

**도입안:**
```bash
npm i -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

```js
// vitest.config.ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
export default defineConfig({
  plugins: [react()],
  test: { environment: 'jsdom', globals: true, setupFiles: ['./src/test-setup.ts'] }
});
```

**최소 테스트 셋:**
1. `DashboardView.test.jsx` — diary-tx source_id 중복 합산 회귀 방지
2. `HistoryView.test.jsx` — 자정 직후 selectedDate 표시 검증
3. `BudgetView.test.jsx` — runLocalAI(allowanceTotal) 누락 회귀 방지
4. `validate.test.ts` — 스키마 strict 모드 throw 검증

**CI 설정:**
```yaml
# .github/workflows/test.yml
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npx vitest run
```

> **[claude]** Vercel 배포 게이트로 PR 단위 테스트 강제. 17단계 산출물 보호 효과 즉시.

---

## 추가 요청 사항 기록 공간

> 이 섹션에 추가 요구사항을 기록해 주세요.

- [ ] 항목 1
- [ ] 항목 2

---

## 수정 우선순위 요약 (rev.3)

| 순위 | ID     | 파일                        | 내용                                          | 난이도 | 출처       |
|------|--------|-----------------------------|-----------------------------------------------|--------|------------|
| P0   | P0-1   | DashboardView.jsx           | 이중 지출 카운팅 제거                         | 낮음   | 코드 분석  | [완료]
| P0   | P0-2   | App.jsx + supabase.js       | SOS 채널 unsubscribeSos 추가                  | 낮음   | 코드 분석  | [완료]
| P0   | P0-3   | public/sw.js                | diary 타입은 큐 유지로 위임                   | 낮음   | 코드 분석  | [완료]
| P0   | P0-4   | InputSheet.jsx              | 날짜 선택 기능 추가 (소급 입력)               | 낮음   | wody 요청  | [완료]
| P0   | P0-5   | offlineIDB.js + App.jsx     | quota 체크 + 데이터 소실 방어                 | 중간   | claude     |
| P1   | P1-1   | HistoryView.jsx             | 날짜 오프셋 이중 오류 수정                    | 낮음   | 코드 분석  | [완료]
| P1   | P1-2   | App.jsx + DetailSheet.jsx   | editDiaryWithTx + deleteDiaryWithTx           | 중간   | 코드 분석  |
| P1   | P1-3   | DiaryView + DashboardView   | 자정 날짜 고정 버그 + visibility 갱신         | 낮음   | 코드 분석  | [완료]
| P1   | P1-4   | InputSheet.jsx + image.js   | 사진 압축 강화 (1단계)                        | 낮음   | 코드 분석  |
| P1   | P1-5   | App.jsx                     | handleDiarySave 부분실패 보상                 | 중간   | claude     |
| P2   | P2-1   | App.jsx                     | setter deps 최적화 (saveTriggerRef 패턴)      | 중간   | 코드 분석  |
| P2   | P2-2   | validate.js                 | strict 모드 + dev/prod 분기                   | 낮음   | 코드 분석  |
| P2   | P2-3   | supabase_setup.sql + supabase.js | household_data RLS + set_household_id RPC | 중간   | 코드 분석  |
| P2   | P2-4   | constants/index.js          | deprecated 변수 완전 제거                     | 낮음   | 코드 분석  |
| P2   | P2-5   | App.jsx                     | 타이머 누수 일제 점검                         | 중간   | claude     |
| P5   | P5-1   | SettlementView.jsx          | 급여 필드 라벨 재정의 + salary 마이그레이션   | 낮음   | wody 요청  | [완료]
| P6   | P6-1   | BudgetView.jsx + constants  | 입력자(who) 필드 추가 및 표기                 | 낮음   | wody 요청  |
| P6   | P6-2   | BudgetView.jsx              | 할부 종료일 계산 표시                         | 낮음   | wody 요청  |
| P6   | P6-3   | BudgetView.jsx              | 할부 간트 차트 (월별 막대 + 강조)             | 중간   | wody 요청  |
| P7   | P7-1   | SettlementView.jsx          | getBillingPeriod 기반 카드별 참고값 + 자동 채움 | 낮음 | wody 요청  |
| P7   | P7-2   | BudgetView.jsx + api/       | AI 예산 allowanceTotal 누락 버그 수정         | 낮음   | wody 요청  | [완료]
| P3   | P3-1~5 | UI/UX 고도화                | Antigravity + claude 제안 5종                 | 중간   | 합동       |
| P4   | P4-1~3 | 시스템 안정성               | 에러 리포팅, 백업, 리팩토링                   | 높음   | Antigravity|
| P8   | P8-1   | vitest + CI                 | 테스트 자동화 인프라                          | 중간   | claude     |

---

## 구현 진행 상황 (claude)

**Phase 4 (부분 완료, 2026-04-27 KST):**
- [완료] P3-1 다이어리 검색 — DiaryView 상단 검색바 + 메모/항목라벨 매칭
- [완료] P3-3 지출 항목별 삭제 — InputSheet에 이미 구현되어 있음 (검증)
- [완료] P4-1 ErrorBoundary 자동 리포팅 — db.reportBug 연동 + hasReportedRef 가드
- [보류] P3-2 Optimistic Locking — updated_at 스키마 추가 필요 (별도 마이그레이션)
- [보류] P3-4 SyncBar 큐 개수 — 기존 syncStatus dot으로 충분, 향후 IDB count polling 별도
- [보류] P3-5 다이어리 PDF/이미지 export — html2canvas/jspdf 의존성 추가 별도
- [보류] P4-2 데이터 백업/복원 — 별도 UI/마이그레이션 작업
- [보류] P4-3 컴포넌트 리팩토링 — 별도 PR로 (custom hook 추출)

**Phase 3 (완료, 2026-04-27 KST):**
- [완료] P2-3 RLS + set_household_id RPC — supabase_setup.sql + setHouseholdContext + loadShared 호출
- [완료] P2-1 setter deps 최적화 — _makeSetter 패턴 (functional update + queueMicrotask)
- [완료] P2-4 NOW/DAYS deprecated 제거 — constants 5개 export 삭제
- [완료] P2-5 타이머 누수 일제 점검 — saveTimers cleanup useEffect
- [완료] P6-3 할부 간트 차트 — 12개월 grid + 현재월 강조 + who 색상 + 종료임박 표시 + 합계
- [완료] P8 vitest 인프라 — vitest.config.js + 4개 테스트 + .github/workflows/test.yml

타입체크: tsc --noEmit 0 errors

**Phase 2 (완료, 2026-04-27 KST):**
- [완료] P1-2 editDiaryWithTx + deleteDiaryWithTx — Context API 노출 + 롤백 가드
- [완료] P1-4 사진 압축 강화 — image.js 600px/0.65, InputSheet 2장 제한
- [완료] P0-5 IDB quota 가드 — assertStorageQuota 85% 임계 + addToast
- [완료] P1-5 handleDiarySave 부분실패 보상 — try/catch + diary 롤백 토스트
- [완료] P2-2 validate strict — dev throw / prod console.error
- [완료] P6-1/P6-2 FixedItem who + 할부 종료일 — typedef + UI + 라벨 버그 수정 (name → label)
- [완료] P7-1 카드 청구 참고값 + 자동 채움 — getBillingPeriod + install/fixed 합산 + 행별 채움 버튼

타입체크: tsc --noEmit 0 errors

**Phase 1 (완료, 2026-04-27 KST):**
- [완료] P0-1 DashboardView 이중 카운팅 제거 — tx 단일소스 + 고아 tx 리스트 통합
- [완료] P0-2 SOS 채널 unsubscribe — supabase.js unsubscribeSos / unsubscribeAll + App.jsx cleanup
- [완료] P0-3 sw.js diary 큐 유지 — App에 위임 (race condition 회피)
- [완료] P0-4 InputSheet 날짜 선택 — entryDate state + 미래 차단 + 1년 초과 confirm + 소급 시 12:00
- [완료] P1-1 HistoryView 타임존 — helpers.parseLocalDate 신규 + toDateStr 적용
- [완료] P1-3 자정 날짜 고정 — 모듈 const 제거 + visibilitychange 갱신 (DiaryView/DashboardView/InputSheet)
- [완료] P5-1 Settlement 급여 라벨 + salary 마이그레이션 — fallback 적용
- [완료] P7-2 AI allowanceTotal 누락 — runLocalAI/runAI/api/budget-ai.js 동시 + 정규화 안전망

타입체크: tsc --noEmit 0 errors

## 권장 구현 순서 (claude)

**Phase 1 (즉시, 1~2일):**
P0-1 → P0-2 → P0-3 → P0-4 → P1-1 → P1-3 → P5-1 → P7-2
- 데이터 무결성/메모리 누수/wody 시급 요청 우선

**Phase 2 (1주):**
P1-2 → P1-4 → P1-5 → P0-5 → P2-2 → P6-1 → P6-2 → P7-1

**Phase 3 (2~3주):**
P2-3 (RLS 단계적 배포) → P2-1 → P2-4 → P2-5 → P6-3 → P8 (테스트 인프라)

**Phase 4 (장기):**
P3 + P4 항목

---

*본 계획서는 rev.2(2026-04-26 10:15) 기반으로 claude가 실제 src/ 코드와 PROJECT_HISTORY.md 17단계까지를 1:1 대조 검증한 결과물입니다.*
*검증 시각: 2026-04-27 02:03 KST.*
