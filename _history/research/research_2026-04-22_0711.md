# 프로젝트 종합 현황 보고서

**작성일시**: 2026-04-22 07:11
**분석 범위**: 전체 폴더 (_history 22개 파일, 루트 설정 파일, 소스 구조)
**목적**: 프로젝트 전체 흐름 파악 및 현재 상태 명확화

---

## 1. 프로젝트 정체성

**이름**: showmethemoney (가족/부부용 실시간 가계부)
**사용자**: 우진(나) + 지연(파트너), 두 기기 간 실시간 동기화
**배포 URL**: https://showmethemoney-eta.vercel.app
**Vercel 프로젝트 ID**: `prj_Gnpqh43JOTTT4odEpQbsUQ30WyPa`

---

## 2. 코드베이스 이중 구조

이 프로젝트에는 **두 개의 독립적인 앱 구현체**가 공존한다.

### 2-1. 모듈형 앱 (V3/V4 — src/)

React 18 + Vite 5 + Supabase + Vercel Serverless 기반 풀스택 앱.
**현재 src/ 디렉토리에 소스 파일 없음** (이전 작업에서 제거되었거나 git에서만 관리 중).
package.json은 존재하며 이 앱의 의존성을 명시하고 있다.

```
package.json 주요 의존성:
- react: ^18.2.0
- @supabase/supabase-js: ^2.100.0
- framer-motion: ^12.38.0
- react-grid-layout: ^2.2.3
- recharts: ^2.12.7
- zustand: ^5.0.12
- date-fns: ^4.1.0
- vite: ^5.1.4
```

### 2-2. 단일 HTML 앱 (V4 디자인 핸드오프 — showmethemoney.html)

React 18 + Babel CDN 기반 단일 파일 앱. 빌드 불필요.
**현재 활성 개발 대상**. 이 파일이 Vercel에 배포되어 있다.

```
배포 구조:
showmethemoney.html
├── dist/index.html               (복사본)
└── .vercel/output/static/index.html  (실제 배포 대상)

배포 명령: npx vercel deploy --prebuilt --prod
```

---

## 3. 프로젝트 발전 단계 (12단계 전체)

### 1단계 (04.06~04.10) — 기반 구축
React+Supabase+Vercel 아키텍처 확립. PWA + Share Target API. react-grid-layout 위젯 대시보드. Gemini AI OCR/Nudge 초기 구현.

### 2단계 (04.13) — 코드 감사 및 DB 최적화
전체 소스 감사. JSONB → 정규화 transactions 테이블 전환 계획. 오프라인 큐 안정성 검증. budget-v2 중복 폴더 정리 가이드 수립.

### 3단계 (04.14~04.15) — AI 엔진 고도화
Gemma 4 31B 메인 엔진 채택. 이미지 전처리(흑백변환 800px). 근무 코드 M(M12) 정식 반영. 근무표 수기 파서 완성. 크래시 이슈 해결.

### 4단계 (04.15~) — V4.0 심층 분석
Antigravity 20섹션 분석 보고서. 3가지 테마(dark/light/oldschool). 관리자 모드. 개인정보 보호. Kids Mode Zustand 격리.

### 5단계 (04.16) — Bulk OCR 고도화
다크모드 카드 스크린샷 인식 실패 해결. 컬러 보존 모드. WebP/JPEG 가변 압축(용량 50~70% 절감). extractBulkItemsFallback 방어 레이어.

### 6단계 (04.20) — 카드 정산 시스템
household_data에 settlements 키 추가. SettlementView.jsx (245줄) 신설. 월간 카드 결제 과부족 계산. 오프라인 큐 자동 연동. SettlementSummaryWidget 대시보드 위젯.

### 7단계 (04.22 초) — 디자인 핸드오프 구현
showmethemoney-handoff.zip 수령. JSX 문법 버그 수정(잉여 }). showmethemoney.html → Vercel 배포 완료.

### 8단계 (04.22) — MOCK 제거 및 localStorage 연동 계획
localStorage 기반 상태 관리(smtm_* 네임스페이스). getCurrentMonthTx 헬퍼. 핵심 뷰 동적화. SOSView/SettlementView 크래시 방지. ← 아직 미구현, plan_functional_2026-04-22_0213.md에 계획 수립됨.

### 9단계 (04.22) — Supabase 실시간 동기화 계획
@supabase/supabase-js CDN 연동. Channel Broadcast로 파트너 기기 간 실시간 동기화(sync_tx). MiniCalendar/HistoryView 월별 조회. SettingsView 예산 편집 폼.

### 10단계 (04.22) — 데이터 초기화 세분화 계획
ResetSheet 바텀시트. 4가지 범주(지출/가족설정/결제수단/보안) 개별 선택 삭제.

### 11단계 (04.22) — 캘린더 일정 관리 계획
smtm_calendar_notes: 문자열 → 객체 배열({id, text, done}) 하위호환 마이그레이션. 일정 추가/삭제/체크/수정 UI. 실시간 동기화(sync_notes).

### 12단계 (04.22) — 초대 안정화 + TypeScript 완성 (모듈형 앱)
실시간 채널 구독 누수 수정(db.unsubscribe). 역할 중복 합류 방지. tsc 빌드 에러 0건. COMPLETED.

---

## 4. 파일 인벤토리

### 4-1. 루트 파일

| 파일 | 역할 |
|------|------|
| `showmethemoney.html` | 현재 활성 앱 (CDN React, ~98KB, 1533줄) |
| `package.json` | 모듈형 앱 의존성 (family-budget-app v2.0.0) |
| `vercel.json` | SPA 라우팅 설정 |
| `PROJECT_HISTORY.md` | 12단계 마스터 이력 |

### 4-2. _history/ 문서 22개

**분석 보고서**: research_2026-04-07, research_2026-04-13 (×2), research_2026-04-15, research_2026-04-22 (본 문서)

**계획서**:
- `plan_2026-04-07_1523.md` — 홈화면 위젯 통합
- `plan_2026-04-07_1614.md` — 대시보드 위젯 추가
- `plan_2026-04-07_1804.md` — 429 방어 및 캐시 전략
- `plan_expense-entry_2026-04-16_0120.md` — OCR 속도 개선
- `plan_2026-04-20_0932.md` — 카드 정산 시스템 설계
- `plan_2026-04-20_2301.md` — 유지보수 3건
- `plan_final_2026-04-20.md` — 위 두 계획 통합 확인서
- `plan_functional_2026-04-22_0213.md` — showmethemoney.html 기능화 계획 (미구현)
- `plan_invite_fix_2026-04-22_1540.md` — 모듈형 앱 채널 누수 (COMPLETED)
- `plan.md` — 55KB 초기 대형 계획서 (uiux guide 기반)

**구현 문서** (implementation_plan*.md × 6개): 모두 AI OCR 관련, Gemma 4/M12 관련.

**인계 문서**: HANDOVER_V3.md (모듈형 앱), HANDOVER_V4_2026-04-22.md (HTML 배포)

---

## 5. showmethemoney.html 내부 구조

### 5-1. 컴포넌트 맵 (라인 기준)

| 라인 | 컴포넌트 | 문제점 |
|------|---------|--------|
| L44-64 | `MOCK` 전역 상수 | 모든 하드코딩의 근원 |
| L150-188 | `TxRow` | 삭제/수정 UI 없음 |
| L191-229 | `AllowanceCard` | {name:'나', total:300000, spent:155000} 하드코딩 |
| L232-304 | `MiniCalendar` | startDay=3, totalDays=30, selected=22 고정 |
| L308-420 | `HomeView` | MOCK 직접 참조, '200만' 라벨 고정, SOS 배너 항상 표시 |
| L423-538 | `InputSheet` | 저장 버튼 → onClose()만 호출, 날짜 하드코딩 |
| L541-583 | `HistoryView` | MOCK.transactions 참조, '4월 지출 합계' 하드코딩 |
| L586-661 | `PrivateView` | const CORRECT = '1234' 변경 불가 |
| L664-733 | `SOSView` | React.useState(MOCK.sosMessages) → MOCK 제거 시 크래시 |
| L735-918 | `SettlementView` | const s = MOCK.settlement → MOCK 제거 시 크래시 |
| L920-1144 | `OnboardingView` | DEMO_CODE='A1B2C3', X7K9M2 하드코딩 |
| L1221-1278 | `FixedCostsPage` | useState만, localStorage 저장 없음 |
| L1280-1334 | `CardManagementPage` | useState만, localStorage 저장 없음 |
| L1386-1478 | `SettingsView` | 초대코드 X7K9M2 하드코딩, PIN 저장 시 lsSet 누락 |
| L1481-1528 | `App` | transactions 상태 없음, sheetKey 없음 |

### 5-2. 디자인 토큰

```css
--primary: #1C2B4A    /* 나 — Deep Navy */
--secondary: #7A9E87  /* 지연 — Sage Green */
--danger: #E8715A
--bg: #F4F6F8
--surface: #FFFFFF
--radius-lg: 24px / --radius-xl: 32px
```

### 5-3. localStorage 네임스페이스 (구현 계획)

```
smtm_transactions      지출 내역 배열 (JSON)
smtm_budget            예산 설정 (number)
smtm_allowance_me      내 용돈 (number)
smtm_allowance_partner 파트너 용돈 (number)
smtm_fixed_costs       고정비 배열 (JSON)
smtm_cards             카드 배열 (JSON)
smtm_pin               PIN (string)
smtm_invite_code       초대 코드 (최초 1회 생성 후 유지)
smtm_calendar_notes    날짜별 메모 {YYYY-MM-DD: [{id, text, done}]}
```

---

## 6. 핵심 헬퍼 함수 패턴 (계획 코드)

```js
const ls = (k) => localStorage.getItem(k);
const lsSet = (k, v) => localStorage.setItem(k, v);

const toDateKey = (d) => {
  const dt = d instanceof Date ? d : new Date(d);
  return dt.getFullYear()+'-'+String(dt.getMonth()+1).padStart(2,'0')+'-'+String(dt.getDate()).padStart(2,'0');
};

const displayDate = (dateKey) => {
  const today = toDateKey(new Date());
  const yd = new Date(); yd.setDate(yd.getDate()-1);
  const yesterday = toDateKey(yd);
  if (dateKey === today) return '오늘';
  if (dateKey === yesterday) return '어제';
  const [,m,d] = dateKey.split('-');
  return parseInt(m)+'월 '+parseInt(d)+'일';
};

const getCurrentMonthTx = (transactions) => {
  const now = new Date();
  const prefix = now.getFullYear()+'-'+String(now.getMonth()+1).padStart(2,'0');
  return transactions.filter(t => t.date.startsWith(prefix));
};

const generateInviteCode = () => {
  const chars = 'ABCDEFGHJKLMNPQRSTUVWXYZ23456789';
  return Array.from({length:6}, () => chars[Math.floor(Math.random()*chars.length)]).join('');
};

const loadOrGenInviteCode = () => {
  const saved = ls('smtm_invite_code');
  if (saved) return saved;
  const code = generateInviteCode();
  lsSet('smtm_invite_code', code);
  return code;
};
```

---

## 7. 모듈형 앱 핵심 아키텍처 (역사적 참고)

### 7-1. Supabase 테이블

| 테이블 | 역할 |
|--------|------|
| `household_data` | JSONB KV 스토어 (tx_YYYY, fixed, install, cards, plan 등) |
| `transactions` | 정규화 거래 테이블 (집계용, 마이그레이션 후 병행 운영) |
| `sos_requests` | SOS 가불 (trg_sos_approved 트리거 → transactions 자동 INSERT) |
| `kids_profiles` | 아이 프로필 (Zustand kidsStore로 격리) |
| `kids_missions` | 아이 미션 |

### 7-2. AI API 체인

```
OCR 엔진 순위:
  1. gemma-4-31b-it (Gemma 4 31B) — 비용 무료, AbortSignal 12초
  2. gemini-2.5-flash — 429/503 시 폴백, AbortSignal 20초
  3. gemini-2.0-flash — 최종 폴백

Nudge 엔진:
  gemini-2.0-flash-lite — 24시간 Vercel KV 캐시 + sessionStorage 캐시
```

---

## 8. 미해결 과제 우선순위

### showmethemoney.html (현재 배포 중)

| 우선순위 | 문제 | 파일 위치 |
|---------|------|----------|
| 🔴 P1 | SOSView MOCK.sosMessages 크래시 | L665 |
| 🔴 P1 | SettlementView MOCK.settlement 크래시 | L737 |
| 🔴 P2 | InputSheet 저장 버튼 미연결 | L534 |
| 🔴 P2 | App에 transactions 상태 없음 | L1481 |
| 🟡 P3 | 입장 코드 A1B2C3 / X7K9M2 하드코딩 | L933, L1131, L1413 |
| 🟡 P3 | PIN '1234' 하드코딩 | L590 |
| 🟡 P4 | TxRow 삭제/수정 없음 | L150 |
| 🟡 P4 | MiniCalendar 하드코딩 | L232 |
| 🟡 P4 | AllowanceCard 하드코딩 | L191 |
| 🟢 P5 | FixedCostsPage/CardManagementPage 저장 없음 | L1221, L1280 |

전체 계획: `_history/plan_functional_2026-04-22_0213.md` (16개 항목)

---

## 9. 문서 정리 권장

현재 혼재 상황:

1. `src/` 디렉토리 없음 — 모듈형 앱 소스 실종. showmethemoney.html이 실질적 현재 앱.
2. `plan.md` (55KB) — 초기 대형 계획서. 이후 계획서들로 분화되었으므로 참고용으로만.
3. `implementation_plan` 5개 (04.07 당일) — 동일 작업 반복 수정본. 최신 implementation_plan2052.md가 마스터.
4. `research_2026-04-13` 두 버전 — 같은 날 작성, research_1221이 보완본.
5. `node_modules/` — 모듈형 앱 의존성. showmethemoney.html에는 불필요하지만 제거하면 향후 모듈형 복원 시 재설치 필요.

---

## 10. 다음 작업 권장 순서

1. **즉시**: `plan_functional_2026-04-22_0213.md` 기반 showmethemoney.html 구현
   - P1 크래시 방지 → P2 저장 연결 → P3 보안 → P4 UX

2. **배포**: `npx vercel deploy --prebuilt --prod` (dist/index.html 복사 후)

3. **방향 결정**: src/ 모듈형 앱 복원 vs showmethemoney.html 영구 메인화

---

*이 보고서는 2026-04-22 전체 파일을 실제 읽어 작성한 결과입니다.*
