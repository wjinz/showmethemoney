# 안정적인 무료 티어 활용 및 429 오류 방어 최적화 계획

현재 발생하는 429(Too Many Requests) 이슈를 근본적으로 방어하기 위해 제안받은 구조를 바탕으로 실제 코드베이스 검토를 마친 뒤 수립된 구체화 계획입니다.

## 1순위: 모델 다운그레이드 (빠른 RPM 한도 확보)
현재 사용 중인 모델은 오버킬(Overkill)이며 무료 티어 한도(Quota)가 엄격합니다. 특히 많은 양의 트래픽을 차지하는 OCR과 Nudge 모두 무료 티어에서 가장 가성비가 높은 `gemini-2.0-flash-lite`로 통일합니다.

- **`api/nudge.js`**
  ```javascript
  // 기존: const GEMINI_MODEL = 'gemini-2.5-flash';
  const GEMINI_MODEL = 'gemini-2.0-flash-lite';
  ```
- **`api/ocr.js`**
  ```javascript
  // 기존: const GEMINI_MODEL = 'gemini-2.0-flash';
  const GEMINI_MODEL = 'gemini-2.0-flash-lite';
  ```

## 2순위: Nudge API 캐시 TTL 연장
Vercel KV 캐시 생명주기를 1시간에서 24시간으로 대폭 늘려 하루 내의 중복 요청을 방어합니다. 홈 화면의 상태(5% 버킷)가 변하지 않으면 서버는 즉각 200만 응답하므로 AI 토큰 비용을 완전히 아낄 수 있습니다.

- **`api/nudge.js`** 수정
  ```javascript
  // 기존: try { await kv.set(cacheKey, text, { ex: 3600 }); } catch {}
  try { await kv.set(cacheKey, text, { ex: 86400 }); } catch {} // 24시간
  ```

## 3순위: 클라이언트 세션 스토리지 캐시 스택 구성
서버로 접근하는 Vercel 함수 호출 자체를 차단하기 위해 `HomeView.jsx` 단에 sessionStorage 캐싱 로직을 추가합니다.

- **`src/views/HomeView.jsx`**
  ```javascript
  // 수정 방향: fetch 전에 sessionStorage를 확인하는 로직 추가
  const pctBucket = Math.floor(ringPct / 5) * 5;
  const sessionKey = `nudge-${curMonthPrefix}-${pctBucket}`;
  const localCache = sessionStorage.getItem(sessionKey);
  if (localCache) {
    setNudgeText(localCache);
    return;
  }
  // fetch 로직 진행 후 받아온 텍스트를: sessionStorage.setItem(sessionKey, message);
  ```

## 4순위: 이미지 최적화 강화
토큰 입력 사이즈를 대폭 줄여서 처리량 감소 및 속도를 향상시킵니다. 영수증/화면 캡쳐는 1024px로도 글자 인식이 충분합니다.

- **`src/utils/ocr.js`**
  ```javascript
  // 기존: const MAX_SIZE = 1600;
  const MAX_SIZE = 1024;
  ```

## 5순위: 429 에러 대응 폴백(Fallback) 체인
혹시라도 `gemini-2.0-flash-lite`가 한도에 도달하면 무료 할당량이 독립적인 `gemini-1.5-flash-8b` 모델을 대체재로 사용하여 재요청(Auto Retry)하도록 로직을 구현합니다.

- **`api/ocr.js`**
  - fetch 호출을 별도 헬퍼 함수 `callGemini(modelName)`로 분리.
  - `const res = await callGemini('gemini-2.0-flash-lite');` 실행 후 `res.status === 429`이면 다시 `callGemini('gemini-1.5-flash-8b')` 호출하는 구조로 변경.

## *중요: 버그 픽스 (Nudge 응답 브릿지 연결 오류)
클라이언트와 서버의 JSON 키 미스매치로 홈 화면에서 조언이 나타나지 않던 문제를 바로잡습니다. 현재 `nudge.js`는 `{ message: text }`를, `HomeView`는 `data.text`를 원합니다.

- **`src/views/HomeView.jsx`**
  ```javascript
  // 수정 전
  // .then(data => { if (data?.text) setNudgeText(data.text); })
  
  // 수정 후 
  .then(data => { if (data?.message) setNudgeText(data.message); })
  ```
