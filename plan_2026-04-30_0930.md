# 다이어리 데이터 유실 문제 해결 계획 (Plan)
**작업 일시:** 2026-04-30 09:30:56
**Claude 보강:** 2026-04-30 (코드 검증 + 근본 원인 재정의)

## 1. 문제 정의
- **증상:** 사진이 포함된 다이어리 게시 시 기존 게시물을 포함한 전체 목록이 사라짐 (지출 내역은 정상 유지).
- **가설:** 
    1. Base64 사진 데이터로 인해 `diaries` JSONB 배열의 크기가 한도를 초과하여 Supabase 저장 실패.
    2. 저장 실패 후 실시간 동기화 과정에서 로컬 상태가 서버의 빈 데이터(또는 실패 상태)로 덮어씌워짐.
    3. `localStorage` 용량 초과로 인한 오프라인 큐 작동 불능.

<!-- Claude (2026-04-30): 가설 재정의 — 코드 검증 결과 1+2가 결합되어 발생
  진짜 1순위 원인: Realtime echo + payload truncation
    - 사진 포함 diaries 배열이 약 256KB를 초과하면 Supabase Realtime broadcast의
      payload.new.value가 null/undefined 또는 잘린 값으로 client에 도착
    - App.jsx:181 `setDiariesRaw(Array.isArray(value) ? value : [])`이 즉시 빈 배열로 덮어씀
    - 즉 'tx는 정상, diaries만 사라짐' 패턴은 tx는 transactions 테이블(개별 row)이라 echo 영향 없음과 일치
  2순위 원인: setShared의 저장 자체는 성공해도 self-echo 수신 → 빈 배열 set → 다시 dirty → 재저장 루프 가능
  3순위 원인: _makeSetter의 closure race로 빠르게 두 번 setDiaries 호출 시 첫 번째 변경 손실 -->

## 2. 조사 및 검증 (COT 과정)
- `App.jsx:181`: `case 'diaries': setDiariesRaw(Array.isArray(value) ? value : []);` — 서버에서 잘못된 값이 오면 즉시 빈 배열로 초기화됨.
- `App.jsx:517`: `setShared` 함수가 전체 배열을 매번 Supabase에 `upsert` 함. 데이터가 커질수록 실패 확률 증가.
- `src/utils/image.js`: 현재 600px, 0.65 품질로 압축하지만 누적되는 다이어리를 감당하기엔 단일 JSONB 방식은 위험함.

<!-- Claude (2026-04-30): 추가 검증 포인트
  - supabase.js:163: postgres_changes UPDATE/INSERT 모두 자기 client의 변경도 echo로 받음.
    클라이언트 자기 자신의 broadcast 무시 로직이 전혀 없음
  - supabase.js:174: DELETE 핸들러가 `onUpdate(key, [], true)` — 실수로 row가 삭제되면 즉시 클라이언트에서 비워짐
  - App.jsx:611 _makeSetter: `let next` closure → 같은 setter 빠른 재호출 시 next 덮어쓰기 race
  - App.jsx:776: 오프라인 시에만 IDB 큐로 보내고 온라인 시에는 setShared만 호출 → setShared가 실패해도 fallback 백업 없음
  - InputSheet.jsx:122: compressImage catch가 console.warn만 하고 swallow → 사용자 인지 불가 -->

## 3. 단계별 해결 전략

### 1단계: 데이터 안정성 확보 (즉시 적용)
- 다이어리 로드 시 데이터가 비어있을 경우(null/undefined) 기존 로컬 데이터를 보호하는 가드 로직 추가.
- `setShared` 실패 시 사용자에게 명확한 오류 메시지를 전달하고, 상태 롤백 방지.

<!-- Claude (2026-04-30): 1단계 보강 항목
  a) updateSharedState의 'diaries' 분기에서 length 감소 시 lastKnownGood과 비교 후
     (서버) > (수신) 이면 무시하고 즉시 db.loadAll로 재조회
  b) 자기 자신이 만든 broadcast 식별 — setShared 호출 시 client local sequence id를 함께 저장,
     payload.new.value의 마지막 item.id가 본인이 방금 추가한 id와 일치하면 onUpdate 무시
     (현재 schema는 id에 sequence가 들어가 있어서 활용 가능)
  c) 저장 직전 IDB에 lastKnownGood snapshot 저장 → 빈 배열 수신 시 즉시 자동 복구 -->

### 2단계: 저장 방식 최적화 (단기 대책)
- `diaries` 전체 배열 저장 방식을 개별 항목 저장 방식으로 전환하는 구조 검토 (장기적으로 `transactions` 테이블처럼 개별 행으로 관리).
- 우선적으로 이미지 데이터를 포함한 전체 배열의 크기를 체크하여 한도 초과 전 경고 발생.

<!-- Claude (2026-04-30): 2단계 보강 항목
  - 단순 size 체크보다 'append-only patch upsert' 패턴이 더 안전:
      diaries 추가 → key='diary_<id>' 단일 row insert (append-only)
      diaries 로드 → key like 'diary_%' 전체 select 후 client에서 정렬
    이 구조면 broadcast payload도 한 row만 작아져 truncation 회피
  - 압축 강화: 600px → 480px, quality 0.65 → 0.55, 그리고 photos 1장으로 제한 (현재 2장)
  - 임시 회피책: setShared('diaries') 호출 직전 photos만 분리해 IDB에 저장하고
    JSONB에는 photo_id 참조만 보관 (Supabase Storage 도입 전 단계) -->

### 3단계: 인프라 개선 (근본 대책)
- **Supabase Storage 도입:** 사진을 Base64로 DB에 직접 넣지 않고, 별도 Storage에 업로드 후 URL만 DB에 저장하도록 변경.
- **다이어리 테이블 정규화:** `household_data` 내의 단일 JSONB가 아닌, 전용 테이블로 마이그레이션.

<!-- Claude (2026-04-30): 3단계 보강 항목
  - Supabase Storage 도입 시 RLS 정책 설계 필수 — household_id 기반 격리
  - orphan 이미지 GC: diary 삭제 트리거에서 storage object도 함께 제거
  - 마이그레이션 시 무중단 전략:
    Step1) diaries 테이블 신설(id, household_id, payload, photos jsonb[], created_at, updated_at)
    Step2) household_data.diaries 읽기 + 신 테이블 동시 쓰기 (이중 쓰기 기간)
    Step3) 신 테이블 read 전환 + dual-write 종료
    Step4) household_data.diaries row 정리 (반드시 백업 후) -->

## 4. 코드 수정 제안 (스니펫)

### A. App.jsx - 데이터 동기화 가드 강화
```javascript
// src/App.jsx:181 수정 제안
case 'diaries': 
  if (Array.isArray(value)) {
    setDiariesRaw(value); 
  } else if (value === null || value === undefined) {
    // 서버에서 명시적으로 null이 오더라도 로컬 데이터를 바로 지우지 않고 확인
    console.warn('[Sync] Received null diaries from server, ignoring to prevent data loss.');
  } else {
    setDiariesRaw([]);
  }
  break;
```

<!-- Claude (2026-04-30): 스니펫 A 보강 — null 가드만으로는 부족
  truncation 시 빈 배열([])로 잘려 도착할 수 있음. 따라서 length 비교 가드도 필요.

  case 'diaries': {
    if (!Array.isArray(value)) {
      console.warn('[Sync] diaries non-array received, ignored:', typeof value);
      return;
    }
    setDiariesRaw(prev => {
      // 서버 값이 로컬보다 현저히 작으면 truncation 의심 → 무시 후 강제 재조회
      if (value.length === 0 && prev.length > 0) {
        console.warn('[Sync] diaries empty echo while local has', prev.length, '— refetch');
        db.loadAll(householdId).then(d => {
          if (Array.isArray(d.diaries) && d.diaries.length >= prev.length) {
            setDiariesRaw(d.diaries);
          }
        }).catch(e => console.error('[Sync] refetch fail:', e));
        return prev;
      }
      // 마지막 item id가 prev의 마지막 id와 같거나 더 작으면 (= 자기 echo) 무시
      const incomingLastId = value[value.length - 1]?.id ?? 0;
      const localLastId = prev[prev.length - 1]?.id ?? 0;
      if (incomingLastId < localLastId) {
        console.warn('[Sync] diaries stale echo, ignored');
        return prev;
      }
      return value;
    });
    break;
  } -->

### B. handleDiarySave - 저장 전 크기 검사 및 예외 처리
```javascript
// src/App.jsx:755 부근 수정 제안
const handleDiarySave = useCallback((draft) => {
  // ... 기존 diaryItem 생성 로직 ...
  
  // 크기 체크 (예상)
  const estimatedSize = JSON.stringify([...diaries, diaryItem]).length;
  if (estimatedSize > 4 * 1024 * 1024) { // 4MB 초과 시 경고
    addToast("다이어리 데이터가 너무 큽니다. 사진 크기를 줄이거나 기존 일기를 정리해주세요.", "error");
    return;
  }

  setDiaries(prev => [...prev, diaryItem]);
  // ... 후속 tx 저장 로직 ...
}, [diaries, setDiaries, addToast]);
```

<!-- Claude (2026-04-30): 스니펫 B 보강 — 임계값과 측정 시점 수정 필요
  1) 4MB 한도는 너무 늦음. Realtime broadcast가 약 256KB부터 잘리므로 실제 한도는 그보다 작아야 함
  2) JSON.stringify 전체를 매번 호출하면 비용이 큼 → photos.length * 평균크기로 근사
  3) 측정값이 한도 초과면 setDiaries 자체를 막고 사용자에게 사진 압축/삭제 유도
  4) deps에 diaries 추가 → 매 다이어리 저장마다 함수 재생성됨, useRef로 최신 diaries 보관 권장

  const diariesRef = useRef(diaries);
  useEffect(() => { diariesRef.current = diaries; }, [diaries]);

  const handleDiarySave = useCallback((draft) => {
    const diaryItem = buildDiaryItem(draft);
    const estPayloadKB = estimateRowSize(diariesRef.current, diaryItem);
    if (estPayloadKB > 200) {
      addToast(`저장 한도(200KB) 초과: ${estPayloadKB}KB. 사진을 줄여주세요.`, 'error');
      return;
    }
    setDiaries(prev => [...prev, diaryItem]);
    // 즉시 IDB lastKnownGood snapshot 저장
    idbEnqueue({ type: 'snapshot', householdId, payload: [...diariesRef.current, diaryItem] }).catch(()=>{});
    // ... -->

### C. (Claude 신규) updateSharedState 자기 echo 식별
```javascript
// 각 setShared 호출 시 마지막 sequence를 ref에 기록
const lastWriteSeq = useRef({}); // { diaries: 1234567890123 }

// setShared 내부:
const seq = Date.now();
lastWriteSeq.current[key] = seq;
await db.save(householdId, key, { ...newValue, _seq: seq });

// updateSharedState 내부:
if (value && value._seq && value._seq === lastWriteSeq.current[key]) {
  return; // 자기 자신의 echo는 무시
}
```

<!-- Claude (2026-04-30): _seq를 value에 박는 것이 부담되면
  diaries의 마지막 item.id 자체가 timestamp 기반이므로 그것만 비교해도 충분.
  완벽 식별이 필요하면 supabase의 channel.send에 self-broadcast: false 옵션 활용. -->

### D. (Claude 신규) 저장 실패 시 자동 백업 복원
```javascript
// setShared 내부, db.save 호출 직전:
if (key === 'diaries') {
  await idbEnqueue({
    type: 'snapshot',
    householdId,
    snapshotKey: 'diaries_lkg', // last known good
    payload: newValue,
    ts: Date.now(),
  });
}

// 부팅 시 (loadShared 직후):
const lkg = await idbLoadSnapshot('diaries_lkg', householdId);
if (lkg && Array.isArray(lkg.payload) && lkg.payload.length > diaries.length) {
  console.warn('[recovery] LKG > server, restoring', lkg.payload.length);
  setDiariesRaw(lkg.payload);
  await db.save(householdId, 'diaries', lkg.payload);
}
```

## 5. 검증 계획
1. **재현 테스트:** 고용량 사진을 첨부하여 저장 시 Supabase 네트워크 요청(413 Payload Too Large) 여부 확인.
2. **오프라인 테스트:** 오프라인 모드에서 사진 첨부 후 다시 온라인 전환 시 `flush` 과정에서의 데이터 보존 확인.
3. **용량 모니터링:** Supabase 대시보드에서 `household_data` 테이블의 단일 행 크기 모니터링.

<!-- Claude (2026-04-30): 검증 계획 보강
  4. Realtime 페이로드 truncation 재현:
     - DevTools > Network > WS 탭에서 supabase realtime 메시지 확인
     - 사진 포함 diaries 저장 직후 echo 받은 payload.new.value의 실제 길이/형태 로깅
     - 약 256KB 경계에서 null/잘림 발생하는지 확인
  5. 자기 echo 식별 검증:
     - setDiaries 호출 후 setDiariesRaw가 같은 데이터로 두 번 호출되는지 모니터링 (의도치 않은 중복 set)
  6. 멀티 디바이스 시나리오:
     - 디바이스 A에서 사진 다이어리 저장 → 디바이스 B에서 즉시 확인 (정상 수신 여부)
     - 디바이스 B의 기존 diaries가 사라지지 않는지 확인
  7. 자동화 회귀 테스트(vitest):
     - mock supabase에서 truncated payload broadcast 시뮬레이션
     - updateSharedState가 빈 배열 수신 시 prev를 유지하는지 unit test
-->

## 6. 즉시 적용 가능한 핫픽스 우선순위

<!-- Claude (2026-04-30): 핫픽스 우선순위 — 위에서 아래로 단계적 적용 권장
  [완료] Hotfix-1: App.jsx case 'diaries' 강화 (length 비교 + intentionalReset 플래그 + refetch)
         + supabase.js DELETE 핸들러에서 'diaries' 키는 빈 배열 강제 안 함
  [완료] Hotfix-2: idbSaveDiariesSnapshot/idbLoadDiariesSnapshot 추가
         + setShared('diaries') 직전 LKG 저장
         + loadShared 직후 LKG > server 비교하여 자동 복원
  [보류] Hotfix-3: diaries 항목별 row 분리 / Supabase Storage 도입 (구조 변경 필요, 다음 마일스톤) -->

## 7. 운영 모니터링

<!-- Claude (2026-04-30): 운영 모니터링 추가 권장
  [부분완료] 다음 이벤트는 console.warn / addToast로 전달됨:
      a) [완료] updateSharedState에서 diaries length 감소 감지 → console.warn + auto refetch
      b) [완료] handleDiarySave에서 estimated size > 220KB → addToast('error') + 저장 거절
      c) [완료] compressImage 실패 / 단일 사진 220KB 초과 → addToast('error')
  [보류] Sentry 연동 / 관리자 대시보드 위젯 (다음 마일스톤) -->

## 8. 구현 결과 (2026-04-30)

<!-- Claude (2026-04-30): 본 plan 기반 구현 완료 항목
  [완료] App.jsx: case 'diaries' 강화 + intentionalDiaryReset 플래그
  [완료] App.jsx: _makeSetter closure race 제거 (setterLatest ref 사용)
  [완료] App.jsx: setShared diaries debounce(300ms) 추가
  [완료] App.jsx: setShared diaries 저장 직전 LKG snapshot 저장
  [완료] App.jsx: loadShared 직후 LKG vs server 길이 비교 + 자동 복원
  [완료] App.jsx: handleDiarySave 사이즈 가드(220KB) + diariesRef
  [완료] App.jsx: resetDiaries / resetAll에서 intentionalDiaryReset 플래그 set
  [완료] supabase.js: DELETE 핸들러에서 diaries 키 보호
  [완료] offlineIDB.js: idbSaveDiariesSnapshot / idbLoadDiariesSnapshot + DB v2 마이그레이션
  [완료] InputSheet.jsx: compressImage 220KB 초과 시 재시도 + 실패 toast
  [완료] BudgetContext.jsx: addToast typedef 추가
  [완료] tsc --noEmit 0 errors / vite build 성공
  [대기] vercel deploy: CLI 토큰 부재로 자동 배포 불가, 사용자 토큰 입력 후 `npx vercel deploy --prebuilt --prod` 필요 -->

