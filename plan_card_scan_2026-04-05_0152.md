# 가계부 개선 통합 계획
> 작성일: 2026-04-05 01:52 | 최종 업데이트: 2026-04-05

---

## 목차
1. [카드앱 스크린샷 일괄 지출 입력](#1-카드앱-스크린샷-일괄-지출-입력)
2. [카테고리 예산 탭 개선 + AI 한도 내 추천](#2-카테고리-예산-탭-개선--ai-한도-내-추천)
3. [플랜 요약 탭 개선](#3-플랜-요약-탭-개선)
4. [올드스쿨 테마 교체 — Retro Terminal](#4-올드스쿨-테마-교체--retro-terminal)

---

# 1. 카드앱 스크린샷 일괄 지출 입력
> 상태: 구현 완료 (2026-04-05)

---

## 1. 현황 분석

### 기존 OCR 파이프라인 (이미 구현됨)
```
카메라 촬영 (단일 영수증)
  └─ EntryView.jsx: handleOCR()
       └─ src/utils/ocr.js: runOCR(imageFile)
            └─ POST /api/ocr  (api/ocr.js)
                 └─ Anthropic claude-opus-4-5 → {amount, cat, memo}
                      └─ 폼 자동 입력 (건당 1건)
```

### 문제점
- `api/ocr.js` 프롬프트가 **단일 영수증** 기준으로 설계됨 (총액 1개만 추출)
- 카드앱 스크린샷처럼 **여러 거래가 나열된 이미지**는 처리 불가
- OCR 완료 후 즉시 저장되는 구조라 **복수 건 확인/수정 UI** 없음
- `EntryView.jsx`의 카메라 버튼은 `capture="environment"` 속성으로 카메라만 열림 → 갤러리(스크린샷 선택) 불편

### 기존 DataImportView.jsx와의 차이
- DataImportView: Excel/CSV 파일 → 분석만 (tx 저장 안 함)
- 새 기능: 스크린샷 이미지 → 여러 거래 파싱 → **즉시 tx 저장**

---

## 2. 목표 UX

```
[지출 기록] 탭
  └─ 카메라 버튼 옆 "카드내역" 버튼 추가
       └─ CardScanSheet (bottom sheet) 열림
            ├─ 1. 이미지 선택 (갤러리/카메라)
            ├─ 2. AI 분석 중... 스피너
            ├─ 3. 파싱된 거래 목록 표시
            │    ├─ 각 행: 날짜 / 금액 / 카테고리(수정 가능) / 메모(수정 가능)
            │    └─ 체크박스로 저장할 항목 선택
            └─ 4. "N건 저장" 버튼 → onSave 일괄 호출
```

---

## 3. 변경 파일 목록

| 파일 | 작업 |
|------|------|
| `api/ocr.js` | `mode: "bulk"` 분기 추가 |
| `src/utils/ocr.js` | `runBulkOCR()` 함수 추가 |
| `src/components/CardScanSheet.jsx` | 신규 생성 |
| `src/views/EntryView.jsx` | "카드내역" 버튼 + CardScanSheet 연동 |

---

## 4. 상세 구현

### 4-1. `api/ocr.js` — bulk 모드 추가

기존 `body: { image, mediaType }` → `body: { image, mediaType, mode? }` 로 확장.

```js
// api/ocr.js — 변경 부분만

const { image, mediaType, mode = "single" } = req.body;

const prompt = mode === "bulk"
  ? `이 이미지는 카드 앱의 이용내역 화면 캡처입니다.
화면에 보이는 모든 결제 내역을 추출하세요.

아래 JSON 배열 형식으로만 응답하세요 (코드블록, 설명 없이):
[
  {"date":"YYYY-MM-DD","amount":숫자,"cat":"카테고리","memo":"가맹점명"},
  ...
]

날짜가 없는 항목은 오늘 날짜(${new Date().toISOString().slice(0,10)})를 사용하세요.
취소/환불 내역은 제외하세요.
카테고리: food, housing, education, transport, medical, culture, clothing, sub, etc`
  : `이 영수증 이미지를 분석해서 지출 정보를 추출해주세요.
아래 JSON 형식으로만 응답하세요 (코드블록, 설명 없이 JSON만):
{"amount": 숫자, "cat": "카테고리", "memo": "가게명 또는 설명"}
카테고리: food, housing, education, transport, medical, culture, clothing, sub, etc
금액은 숫자만(원 기호, 콤마 제거).`;

// 응답 파싱 분기
if (mode === "bulk") {
  const match = text.match(/\[[\s\S]*\]/);
  if (!match) return res.status(422).json({ error: "거래 내역을 인식하지 못했습니다." });
  const items = JSON.parse(match[0]);
  const valid = items.filter(i => i.amount > 0);
  return res.status(200).json({ items: valid });
} else {
  const match = text.match(/\{[\s\S]*\}/);
  if (!match) return res.status(422).json({ error: "영수증에서 정보를 인식하지 못했습니다." });
  const result = JSON.parse(match[0]);
  if (typeof result.amount !== "number" || result.amount <= 0) result.amount = null;
  return res.status(200).json(result);
}
```

**방어 코드:**
- `JSON.parse` 실패 시 → 422 반환 (기존 동일)
- `valid.length === 0` → 빈 배열로 반환하되 클라이언트에서 "인식된 항목 없음" 표시
- 날짜 파싱 실패 항목 → 오늘 날짜 fallback (프롬프트에 명시)

---

### 4-2. `src/utils/ocr.js` — runBulkOCR 추가

```js
/**
 * 카드앱 이용내역 스크린샷을 분석해 복수 거래를 반환합니다.
 * @param {File} imageFile
 * @returns {Promise<Array<{date:string, amount:number, cat:string, memo:string}>>}
 */
export async function runBulkOCR(imageFile) {
  return new Promise((resolve, reject) => {
    const reader = new FileReader();

    reader.onload = async (e) => {
      try {
        const dataUrl = e.target.result;
        const base64Image = dataUrl.split(",")[1];
        const mediaType = dataUrl.match(/data:(image\/[^;]+);/)?.[1] ?? "image/jpeg";

        const headers = { "Content-Type": "application/json" };
        const secret = import.meta.env.VITE_INTERNAL_API_SECRET;
        if (secret) headers["x-internal-secret"] = secret;

        const res = await fetch("/api/ocr", {
          method: "POST",
          headers,
          body: JSON.stringify({ image: base64Image, mediaType, mode: "bulk" }),
        });

        const data = await res.json();
        if (!res.ok) throw new Error(data.error ?? `서버 오류 (${res.status})`);

        resolve(Array.isArray(data.items) ? data.items : []);
      } catch (err) {
        reject(err);
      }
    };

    reader.onerror = () => reject(new Error("이미지 파일을 읽을 수 없습니다."));
    reader.readAsDataURL(imageFile);
  });
}
```

---

### 4-3. `src/components/CardScanSheet.jsx` — 신규 컴포넌트

#### 타입 정의
```ts
// ScannedItem (내부 상태)
interface ScannedItem {
  id: string;          // crypto.randomUUID()
  date: string;        // "YYYY-MM-DD"
  amount: number;
  cat: string;
  memo: string;
  selected: boolean;
}
```

#### 상태 설계
```js
const [phase, setPhase] = useState("idle");       // idle | scanning | review
const [items, setItems] = useState([]);            // ScannedItem[]
const [errMsg, setErrMsg] = useState("");
```

#### 단계별 렌더링

**idle**: 이미지 선택 영역
```jsx
<label style={uploadAreaStyle}>
  <input
    type="file"
    accept="image/*"
    // capture 속성 제거 → 갤러리에서 스크린샷 선택 가능
    onChange={handleFile}
    style={{ display: "none" }}
  />
  카드 이용내역 스크린샷 선택
</label>
```

**scanning**: 스피너 + "AI 분석 중..."

**review**: 파싱 결과 목록

```jsx
// 항목별 행
{items.map(item => (
  <div key={item.id} style={rowStyle}>
    <input
      type="checkbox"
      checked={item.selected}
      onChange={() => toggleSelect(item.id)}
    />
    <input
      type="date"
      value={item.date}
      onChange={e => updateItem(item.id, "date", e.target.value)}
    />
    <span>{item.amount.toLocaleString()}원</span>
    <CatSelect
      value={item.cat}
      onChange={v => updateItem(item.id, "cat", v)}
    />
    <input
      value={item.memo}
      onChange={e => updateItem(item.id, "memo", e.target.value)}
    />
  </div>
))}

<button onClick={handleSaveAll} disabled={selectedCount === 0}>
  {selectedCount}건 저장
</button>
```

#### 핵심 함수

```js
const handleFile = async (e) => {
  const file = e.target.files[0];
  if (!file) return;
  setPhase("scanning");
  setErrMsg("");
  try {
    const raw = await runBulkOCR(file);
    if (raw.length === 0) { setErrMsg("인식된 거래가 없어요. 다른 이미지를 시도해보세요."); setPhase("idle"); return; }
    setItems(raw.map(r => ({ ...r, id: crypto.randomUUID(), selected: true })));
    setPhase("review");
  } catch (err) {
    setErrMsg(err.message ?? "OCR 오류"); setPhase("idle");
  }
};

const toggleSelect = (id) =>
  setItems(prev => prev.map(i => i.id === id ? { ...i, selected: !i.selected } : i));

const updateItem = (id, field, value) =>
  setItems(prev => prev.map(i => i.id === id ? { ...i, [field]: value } : i));

const handleSaveAll = () => {
  const toSave = items.filter(i => i.selected);
  toSave.forEach(i => onSave({ who, amount: i.amount, cat: i.cat, memo: i.memo, date: i.date, payMethod: "credit" }));
  onClose();
};
```

---

### 4-4. `src/views/EntryView.jsx` — 최소 변경

```jsx
// 기존 import에 추가
import { CardScanSheet } from "../components/CardScanSheet";

// state 추가 (기존 state 블록 하단)
const [showCardScan, setShowCardScan] = useState(false);

// 카메라 버튼 영역 (기존 label 옆에 버튼 추가)
<div className="u5" style={{ display: "flex", gap: 8, marginBottom: 20 }}>

  {/* 기존: 단일 영수증 카메라 */}
  <label style={cameraLabelStyle}>
    <input type="file" accept="image/*" capture="environment" onChange={handleOCR} style={{ display: "none" }} disabled={isOCR} />
    {isOCR ? <OCRSpinner /> : "📷"}
  </label>

  {/* 신규: 카드내역 스크린샷 */}
  <button
    onClick={() => setShowCardScan(true)}
    style={{
      width: 52, height: 52, borderRadius: 13,
      background: "var(--bg3)", border: "1px solid var(--border)",
      fontSize: 11, fontWeight: 700, color: "var(--text2)", cursor: "pointer",
      display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center", gap: 2,
      flexShrink: 0,
    }}
  >
    <span style={{ fontSize: 16 }}>🪪</span>
    <span>카드</span>
  </button>

  {/* 저장 버튼 (기존 그대로) */}
  <button onClick={save} ...>...</button>

</div>

{/* CardScanSheet */}
{showCardScan && (
  <CardScanSheet
    who={who}
    onSave={onSave}
    onClose={() => setShowCardScan(false)}
  />
)}
```

---

## 5. 구현 순서 (권장)

1. `api/ocr.js` — bulk 분기 추가 (서버)
2. `src/utils/ocr.js` — `runBulkOCR` 추가
3. `src/components/CardScanSheet.jsx` — 신규 생성
4. `src/views/EntryView.jsx` — 버튼 + Sheet 연동
5. 로컬 테스트 → 실 카드앱 스크린샷으로 검증

---

## 6. 주의사항

- **`capture` 속성 제거**: CardScanSheet의 file input에는 `capture` 속성을 붙이지 않아야 갤러리(스크린샷)를 선택할 수 있음. 기존 EntryView의 단일 영수증 버튼은 `capture="environment"` 유지.
- **모델 비용**: bulk 모드는 이미지 1장에 여러 거래를 파싱하므로 API 호출 횟수는 동일하나 입력 토큰이 더 클 수 있음. 현재 `claude-opus-4-5` 사용 중 → `claude-haiku-4-5-20251001`로 교체하면 비용 절감 가능.
- **카드사별 레이아웃 차이**: KB, 신한, 삼성, 현대카드 앱 UI가 다르므로 프롬프트를 범용적으로 유지. 특정 카드사 전용 파싱 로직은 추후 추가.
- **날짜 필드**: 카드 이용내역 스크린샷에 연도가 없는 경우가 많음 → 프롬프트에 현재 연도 주입 필요.

---

# 2. 카테고리 예산 탭 개선 + AI 한도 내 추천
> 상태: 구현 완료 (2026-04-05)

## 문제 정의

**문제 1 — 카테고리 예산 탭이 유명무실**
기존 `BudgetTab`의 카테고리 목록은 예산 금액과 비중 바(%)만 표시. 이달 실제 지출과 비교가 없어 현황 파악 불가.

**문제 2 — AI 추천이 예산 한도를 초과**
`api/budget-ai.js`는 `available = totalSalary - fixedTotal - installTotal - savingsTarget`로 100% 기준 계산.
클라이언트의 `utilizationTarget`(90%, 80% 등)이 AI 호출 시 전달되지 않아 AI가 한도 초과 예산을 추천하는 버그.

**문제 3 — utilizationTarget 선택지 부족**
기존 UI는 90% / 100% 두 버튼만 제공. 사용자 정의 불가.

## 변경 파일

| 파일 | 변경 내용 |
|------|-----------|
| `api/budget-ai.js` | `utilizationTarget` 파라미터 수신, `available` 계산에 반영 |
| `src/views/BudgetView.jsx` | `runLocalAI` 파라미터 추가, BudgetTab UI 전면 개선 |

## 구현 상세

### `api/budget-ai.js`

```js
// 변경 전
const { totalSalary, fixedTotal, installTotal, savingsTarget, catHistory } = req.body;
const available = Math.max(totalSalary - (fixedTotal||0) - (installTotal||0) - (savingsTarget||0), 0);

// 변경 후
const { totalSalary, fixedTotal, installTotal, savingsTarget, catHistory, utilizationTarget } = req.body;
const utilPct = Math.min(Math.max(utilizationTarget || 100, 50), 100) / 100;
const rawAvailable = Math.max(totalSalary - (fixedTotal||0) - (installTotal||0) - (savingsTarget||0), 0);
const available = Math.round(rawAvailable * utilPct);
```

**방어 코드:**
- `utilizationTarget` 미전달 시 100으로 fallback
- 범위 클램핑: 50~100 (50% 미만은 의미 없으므로)

### `src/views/BudgetView.jsx` — `runLocalAI`

```js
// 변경 전
const runLocalAI = (totalSalary, fixedTotal, installTotal, savingsTarget, catHistory) => {
  const monthlyAvail = Math.max(0, totalSalary - fixedTotal - installTotal - savingsTarget);

// 변경 후
const runLocalAI = (totalSalary, fixedTotal, installTotal, savingsTarget, catHistory, utilizationTarget = 100) => {
  const rawAvail = Math.max(0, totalSalary - fixedTotal - installTotal - savingsTarget);
  const monthlyAvail = Math.round(rawAvail * Math.min(Math.max(utilizationTarget, 50), 100) / 100);
```

### `src/views/BudgetView.jsx` — `BudgetTab` UI

**한도 설정 UI**
```
기존: [90% 지출 타겟] [100% 지출 타겟]

변경: [70%] [80%] [90%] [100%] [직접]
      └─ "직접" 선택 시 슬라이더 노출 (50~100%, 5% 단위)
```

```jsx
// 예산 요약 카드 (신규)
변동비 가용액:      2,000,000원
AI 배분 한도 (80%): 1,600,000원   ← 골드색
버퍼:               400,000원 (예비비 성격)
```

**카테고리 목록 (기존 → 개선)**
```
기존: [아이콘] 식비    비중 35%          ████░░░░  500,000원

변경: ┌─────────────────────────────────┐
      │ 🍽 식비                320,000원 │
      │                        /500,000원 │
      │ ████████████████░░░░░░░          │  진행 바 (사용률 색상 동적)
      │ 사용 64%    남은 예산 180,000원  │
      └─────────────────────────────────┘
```

진행 바 색상 규칙:
- `spentPct < 80` → 카테고리 고유 색
- `spentPct >= 80` → `var(--gold)` (경고)
- `spent > budget` → `var(--red)` (초과) + 카드 테두리도 빨강

**AI 호출 시 `utilizationTarget` 전달**
```js
// 변경 전
body: JSON.stringify({ totalSalary, fixedTotal, installTotal, savingsTarget, catHistory })

// 변경 후
body: JSON.stringify({ totalSalary, fixedTotal, installTotal, savingsTarget, catHistory, utilizationTarget: utilTarget })
```

## 동작 시나리오

```
사용자: 한도를 80%로 설정
  └─ monthlyAvail = rawAvail * 0.8 (클라이언트 표시)
  └─ "AI 추천 시작" 클릭
       └─ POST /api/budget-ai { ..., utilizationTarget: 80 }
            └─ available = rawAvailable * 0.80 (서버 계산)
                 └─ AI가 이 금액 내에서 9개 카테고리 배분
                      └─ 합계 검증 및 보정 (etc에 오차 흡수)
                           └─ 클라이언트: "전체 적용" → budgets 갱신
```

---

# 3. 플랜 요약 탭 개선
> 상태: 구현 완료 (2026-04-05)

## 문제 정의

**버그**: `BudgetView.jsx` 609번 줄에서 "플랜 요약" 탭이 `IncomeTab`을 그대로 렌더링.

```js
// 현재 코드 — 수입/저축 탭과 완전히 동일
{tab === "summary" && <IncomeTab plan={plan} setPlan={setPlan} fixed={fixed} install={install} />}
```

"수입/저축" 탭에서 급여·저축 입력이 가능하고, "플랜 요약" 탭도 동일한 편집 UI를 보여줘서 **탭 자체가 존재 이유가 없음**.

## 방향 결정

탭을 제거하는 대신 **전체 재무 현황을 한눈에 보는 read-only 대시보드**로 재탄생시킨다.

각 탭에 흩어진 정보(수입, 고정비, 예산, 이벤트, 이달 지출)를 압축해서 보여주는 **원페이지 요약 뷰**.

## 목표 레이아웃

```
┌─────────────────────────────┐
│ 월 현금 흐름 요약            │
│ 수입 3,500,000원            │
│   └ 저축    -500,000원      │
│   └ 고정비  -900,000원      │
│   └ 변동예산 2,100,000원    │ ← goldD 강조
└─────────────────────────────┘

┌─────────────────────────────┐
│ 이달 예산 소진 현황          │
│ 식비   ████░░  320k/500k   │
│ 교통   ██░░░░   80k/150k   │
│ ...                         │
│ 전체   ████░░ 1.2M/2.1M    │ ← 총합 진행 바
└─────────────────────────────┘

┌─────────────────────────────┐
│ 고정비 TOP 3                 │
│ 1. 월세      600,000원      │
│ 2. 보험      180,000원      │
│ 3. 인터넷     55,000원      │
└─────────────────────────────┘

┌─────────────────────────────┐
│ 다가오는 이벤트              │
│ 5월 어버이날    200,000원   │
│ 8월 여름휴가  1,500,000원   │
└─────────────────────────────┘
```

## 변경 파일

| 파일 | 작업 |
|------|------|
| `src/views/BudgetView.jsx` | `SummaryTab` 신규 함수 컴포넌트 작성 |
| `src/views/BudgetView.jsx` | `tab === "summary"` 렌더 교체 |

## 구현 상세

### 타입/데이터 흐름

`SummaryTab`은 read-only이므로 setter 불필요. 필요한 props:

```js
function SummaryTab({ plan, budgets, tx, fixed, install }) { ... }
```

### 섹션 1 — 월 현금 흐름 (폭포수 카드)

```js
const salary      = plan.salary || {};
const income      = (salary.husband || 0) + (salary.wife || 0);
const savings     = salary.savingsTarget || 0;
const fixedTotal  = fixed.reduce((s, f) => s + f.amount, 0)
                  + install.reduce((s, i) => s + i.monthly, 0);
const utilTarget  = plan.utilizationTarget ?? 100;
const rawAvail    = Math.max(0, income - savings - fixedTotal);
const avail       = Math.round(rawAvail * utilTarget / 100);
const buffer      = rawAvail - avail;  // utilTarget < 100일 때 예비비
```

렌더: 각 항목을 행으로 나열, 마지막 "변동예산" 행을 goldD 배경으로 강조.

### 섹션 2 — 이달 예산 소진 현황 (미니 바 목록)

```js
const nowYm = `${getYear()}-${String(getMonth()).padStart(2, "0")}`;
const thisMonthTx = tx.filter(t => t.date.startsWith(nowYm));
```

- 카테고리별 `spent / budget` 비율 계산
- 예산 0인 카테고리는 표시하지 않거나 "미설정" 표기
- 전체 합산 바를 맨 하단에 추가

### 섹션 3 — 고정비 TOP 3

`fixed`를 금액 내림차순으로 정렬 후 상위 3개만 표시. `install`도 포함.

### 섹션 4 — 다가오는 이벤트

현재 월 이후의 `plan.events`를 월 오름차순으로 최대 3개 표시. 없으면 섹션 자체 숨김.

### `tab === "summary"` 교체

```js
// 변경 전
{tab === "summary" && <IncomeTab plan={plan} setPlan={setPlan} fixed={fixed} install={install} />}

// 변경 후
{tab === "summary" && (
  <SummaryTab
    plan={plan}
    budgets={budgets}
    tx={tx}
    fixed={fixed}
    install={install}
  />
)}
```

## 고려사항

- **편집 접근성**: 요약 탭은 read-only이므로 각 섹션 우상단에 "수정 →" 링크 버튼을 두고 해당 탭으로 이동하는 `onTabChange` 콜백 받는 방안 검토. 구현 시 `BudgetView`에서 `tab` setter를 SummaryTab에 내려줘야 함.
- **수입 미설정 상태**: `income === 0`이면 "수입을 먼저 입력해주세요" 안내 카드만 표시하고 나머지 섹션 숨김.
- **이달 지출 데이터 없음**: 섹션 2에서 `thisMonthTx.length === 0`이면 "이달 지출 내역이 없어요" 표시.

---

# 4. 올드스쿨 테마 교체 — Retro Terminal
> 상태: 구현 완료 (2026-04-05)

## 문제 정의

### 버그 1 — `.oldschool .card` CSS 규칙이 실제로 적용되지 않음

`globalStyles.js`에서 `.oldschool .card`로 카드 스타일을 재정의하지만, `UI.jsx`의 `Card` 컴포넌트가 `card` 클래스명을 자동 부여하지 않아 선택자 자체가 매칭되지 않는다.

```jsx
// UI.jsx 현재 코드 — className prop을 그대로 씀, "card" 클래스 없음
export function Card({ children, style={}, className="" }) {
  return <div className={className} style={{ background:"var(--bg2)", borderRadius:18, ...style }}>
    {children}
  </div>;
}
```

`borderRadius: 18`이 인라인 스타일로 박혀 있어, CSS `!important`조차 이를 이길 수 없다. 이 때문에 oldschool 모드에서 카드 모서리가 여전히 둥글게 보인다.

### 버그 2 — `border: 2px outset` 이 레이아웃을 밀어냄

`border: 2px outset #fff !important`가 버튼에 적용되면 기존 `1px solid` 대비 1px씩 더 공간을 차지해 인접 요소가 밀려나고 프레임이 어긋나는 현상이 생긴다.

### 디자인 문제 — Win 3.1이 너무 직접적

- `--bg: #008080` (강렬한 틸) + `--bg2: #c0c0c0` (은색) 조합이 앱과 맞지 않음
- `Courier New`에 한글 폴백이 없어 한글이 시스템 기본 폰트로 깨져 보임
- 볼록/오목 버튼(outset/inset)이 모바일 터치에 어색함

---

## 새 방향 — Retro Terminal

Win 3.1 직접 모사 대신 **CRT 터미널 미학**을 세련되게 재해석.

```
컬러: 거의 검은 배경 + Phosphor Amber 단색 포인트
폰트: Space Mono (라틴) + Noto Sans KR (한글 폴백)
형태: 각진 모서리(radius 2px), 1px solid 테두리, flat 버튼
효과: 미세한 scanline 오버레이 (CSS only)
```

### 색상 팔레트

| 변수 | 기존 Win 3.1 | 새 Retro Terminal |
|------|-------------|-------------------|
| `--bg` | `#008080` | `#080a06` |
| `--bg2` | `#c0c0c0` | `#0f120c` |
| `--bg3` | `#dfdfdf` | `#161a12` |
| `--bg4` | `#808080` | `#1e2418` |
| `--border` | `#000000` | `rgba(180,220,80,0.25)` |
| `--border2` | `#808080` | `rgba(180,220,80,0.45)` |
| `--gold` (포인트) | `#808000` | `#b4dc50` (phosphor green-amber) |
| `--goldL` | `#ffff00` | `#d4f070` |
| `--goldD` | dim | `rgba(180,220,80,0.10)` |
| `--text` | `#000000` | `#c8e89a` |
| `--text2` | `#333333` | `#7a9a50` |
| `--text3` | `#555555` | `#3a5a28` |
| `--green` | `#008000` | `#4dab87` |
| `--red` | `#ff0000` | `#d95f5f` |
| `--nav-bg` | `#c0c0c0` | `rgba(8,10,6,0.97)` |

---

## 변경 파일

| 파일 | 작업 |
|------|------|
| `src/styles/globalStyles.js` | `.oldschool` 블록 전면 교체, 폰트 추가, scanline 효과 |
| `src/components/UI.jsx` | `Card`에 `card` className 자동 부여 |
| `src/views/SettingsView.jsx` | 테마 버튼 레이블 변경 |

---

## 구현 상세

### `src/components/UI.jsx` — Card 버그 수정

```jsx
// 변경 전
export function Card({ children, style={}, className="" }) {
  return (
    <div className={className}
      style={{ background:"var(--bg2)", border:"1px solid var(--border)", borderRadius:18, ...style }}>
      {children}
    </div>
  );
}

// 변경 후 — "card" 클래스를 항상 포함
export function Card({ children, style={}, className="" }) {
  const cls = ["card", className].filter(Boolean).join(" ");
  return (
    <div className={cls}
      style={{ background:"var(--bg2)", border:"1px solid var(--border)", borderRadius:18, ...style }}>
      {children}
    </div>
  );
}
```

이 한 줄 수정으로 `.oldschool .card` CSS 선택자가 실제로 매칭된다.

---

### `src/styles/globalStyles.js` — 폰트 import 추가

```js
// 기존 import 줄 교체
@import url('https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&family=Noto+Sans+KR:wght@400;500;600;700&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap');
```

`Space Mono`는 라틴/숫자 영역만 커버하고 한글은 `Noto Sans KR`로 자연스럽게 폴백된다.

---

### `src/styles/globalStyles.js` — `.oldschool` 블록 전면 교체

```css
/* ── 레트로 터미널 (Retro Terminal) ── */
.oldschool {
  --bg:    #080a06;
  --bg2:   #0f120c;
  --bg3:   #161a12;
  --bg4:   #1e2418;
  --border:  rgba(180,220,80,0.25);
  --border2: rgba(180,220,80,0.45);
  --gold:  #b4dc50;
  --goldL: #d4f070;
  --goldD: rgba(180,220,80,0.10);
  --red:   #d95f5f;
  --redD:  rgba(217,95,95,0.12);
  --green: #4dab87;
  --greenD:rgba(77,171,135,0.12);
  --blue:  #5c8de8;
  --blueD: rgba(92,141,232,0.12);
  --pink:  #d97fa8;
  --pinkD: rgba(217,127,168,0.12);
  --purple:#9b7ee0;
  --purpleD:rgba(155,126,224,0.12);
  --text:  #c8e89a;
  --text2: #7a9a50;
  --text3: #3a5a28;
  --h: var(--blue); --hD: var(--blueD);
  --w: var(--pink); --wD: var(--pinkD);
  --nav-bg: rgba(8,10,6,0.97);
  --track: rgba(180,220,80,0.15);
  --dim:   rgba(180,220,80,0.04);
}

/* 폰트: 라틴/숫자 → Space Mono, 한글 → Noto Sans KR 자동 폴백 */
.oldschool,
.oldschool input,
.oldschool button,
.oldschool textarea {
  font-family: 'Space Mono', 'Noto Sans KR', monospace !important;
  letter-spacing: 0.01em;
}

/* 카드: 각진 테두리, 미세한 포인트 컬러 테두리 */
.oldschool .card {
  border-radius: 2px !important;
  border: 1px solid rgba(180,220,80,0.30) !important;
  background: var(--bg2) !important;
  box-shadow: 0 0 0 1px rgba(180,220,80,0.06) !important;
}

/* 버튼: flat + 1px 테두리, outset/inset 제거 */
.oldschool button:not(.fab) {
  border-radius: 2px !important;
  border: 1px solid rgba(180,220,80,0.35) !important;
  background: var(--bg3) !important;
  color: var(--text) !important;
  box-shadow: none !important;
}

.oldschool button:not(.fab):active {
  background: rgba(180,220,80,0.12) !important;
  border-color: var(--gold) !important;
}

/* input: 각진, 내부는 아주 살짝 밝게 */
.oldschool input[type=text],
.oldschool input[type=number],
.oldschool input[type=date],
.oldschool select,
.oldschool textarea {
  border-radius: 2px !important;
  border: 1px solid rgba(180,220,80,0.35) !important;
  background: rgba(180,220,80,0.05) !important;
  color: var(--text) !important;
  caret-color: var(--gold);
}

/* scanline 오버레이 — 앱 루트에 ::after로 적용 */
.oldschool::after {
  content: '';
  position: fixed;
  inset: 0;
  pointer-events: none;
  z-index: 9999;
  background: repeating-linear-gradient(
    to bottom,
    transparent 0px,
    transparent 3px,
    rgba(0,0,0,0.08) 3px,
    rgba(0,0,0,0.08) 4px
  );
}

/* 커서 깜빡임 효과 (선택적) */
.oldschool .serif {
  font-family: 'Space Mono', monospace !important;
  font-style: normal !important;
}
```

---

### `src/views/SettingsView.jsx` — 레이블 변경

```jsx
// 변경 전
{t === "dark" ? "다크 (기본)" : t === "light" ? "라이트" : "Win 3.1 / 386 DOS"}

// 변경 후
{t === "dark" ? "다크 (기본)" : t === "light" ? "라이트" : "레트로 터미널"}
```

---

## 주의사항

- **scanline `::after`와 `z-index`**: `QuickEntrySheet`의 overlay(`z-index: 1000`)보다 낮은 `z-index: 9999`가 역전되지 않도록, `.oldschool::after`의 `pointer-events: none`을 반드시 유지해야 클릭이 통과된다.
- **FAB 버튼**: `button:not(.fab)` 셀렉터로 이미 제외됨. FAB은 골드 그라디언트를 유지.
- **`border-radius` 인라인 스타일 충돌**: `Card`에 `style={{ borderRadius: N }}` 형태의 인라인 오버라이드가 일부 뷰에 있음. CSS `!important`가 인라인 스타일을 이기지 못하므로, Card 컴포넌트 자체의 기본값을 `.oldschool` 모드에서 `0`으로 바꾸는 방식 대신 **Card에 CSS 변수 `--card-radius`를 도입**하는 방안도 고려:
  ```css
  /* globalStyles에서 */
  :root { --card-radius: 18px; }
  .oldschool { --card-radius: 2px; }
  ```
  ```jsx
  /* Card 컴포넌트에서 */
  style={{ borderRadius: "var(--card-radius)", ...style }}
  ```
  이렇게 하면 인라인 스타일도 CSS 변수를 참조하므로 테마 전환이 자연스럽게 작동한다.
- **Ring(SVG) 컴포넌트**: 색상이 이미 CSS 변수 참조이므로 별도 처리 불필요.
