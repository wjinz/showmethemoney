# 프로젝트 심층 분석 보고서 (Research Report)
**작업 일시:** 2026-04-30 09:22:33
**Claude 보강:** 2026-04-30 (코드베이스 검증 완료)

## 1. 아키텍처 개요 (Architecture Overview)

본 프로젝트는 부부 또는 가구 구성원이 실시간으로 가계부를 공유하고 관리할 수 있도록 설계된 **실시간 가계부 PWA**입니다.

### 1.1 기술 스택 (Tech Stack)
- **Frontend:** React 18, Vite, TypeScript(JSDoc 기반 타입 정의)
- **Backend/DB:** Supabase (PostgreSQL, Realtime, RPC)
- **State Management:** React Context (BudgetContext), Zustand (kidsStore)
- **UI/UX:** Vanilla CSS, Framer Motion (애니메이션), Lucide React (아이콘), react-grid-layout (위젯 배치)
- **Utilities:** date-fns, lodash.debounce, xlsx (엑셀 내보내기), Recharts (통계 시각화)

### 1.2 데이터 모델 (Data Model)
데이터는 크게 Supabase의 세 가지 테이블 영역으로 관리됩니다.
- **household_data:** 가계부의 설정, 예산, 고정비, 다이어리 등 대부분의 데이터를 `key-value` (JSONB) 형태로 저장.
- **transactions:** 지출 내역을 정형화된 형태로 저장하여 쿼리 및 통계 성능 최적화.
- **sos_requests / kids_profiles / kids_missions:** 가불 시스템 및 키즈 모드를 위한 전용 테이블.

<!-- Claude (2026-04-30): 데이터 모델 보강 — 다이어리 사진 유실의 1차 원인은 이 구조에서 비롯됨
  - diaries는 household_data 단일 row(id=hid, key='diaries')에 전체 배열을 통째로 JSONB upsert
  - 사진은 base64 dataURL을 photos[]에 inline 보관 (compressImage: 600px / WebP 0.65)
  - PostgreSQL JSONB 컬럼 한도는 1GB이지만 실제로는 다음 한도가 먼저 도달함:
      a) PostgREST request body 한도 (기본 1MB)
      b) Supabase Realtime broadcast payload 한도 (기본 256KB, 정책에 따라 약 3MB까지)
      c) localStorage 한도 (5~10MB) — offlineQueue가 의존
  - tx는 이미 transactions 테이블로 분리되었으나 diaries는 여전히 JSONB 단일 row 패턴 → 동일한 정규화 시급 -->

---

## 2. 핵심 작동 원리 (Core Mechanics)

### 2.1 실시간 동기화 및 상태 관리
- **BudgetContext:** `App.jsx`에서 정의된 모든 상태(`tx`, `budgets`, `plan` 등)와 이를 업데이트하는 함수(`setTx`, `setBudgets` 등)를 하위 컴포넌트에 공급합니다.
- **Supabase Realtime:** `db.subscribe()`를 통해 다른 사용자가 수정한 데이터가 즉시 UI에 반영됩니다. `B10` 규칙에 따라 채널 중복 구독을 방지하고 메모리 누수를 관리합니다.
- **Functional Updates:** 상태 업데이트 시 `prev => next` 패턴을 엄격히 적용하여 연속적인 데이터 입력 시 발생할 수 있는 레이스 컨디션을 방지합니다.

<!-- Claude (2026-04-30): Realtime echo / truncation 분석
  - postgres_changes는 자기 자신이 만든 변경도 그대로 broadcast 받음 (echo)
  - payload.new.value가 한도를 초과하면 null/undefined로 전달되거나 잘려서 옴
  - App.jsx:181 `setDiariesRaw(Array.isArray(value) ? value : [])` — null이면 즉시 빈 배열로 덮어씀
    → 사진 다이어리 저장 직후 본인 클라이언트에서 echo 수신하면서 전체 목록 소실
  - 권장: payload.new.updated_at vs lastSync 비교하여 자기 자신의 echo는 무시,
          또는 payload.new.value가 falsy일 때 db.loadAll로 재조회 후 머지 -->

### 2.2 오프라인 우선 설계 (Offline-First)
- **Offline Queue:** 네트워크가 끊긴 상태에서 발생한 변경사항은 LocalStorage의 `OFFLINE_QUEUE`와 `OFFLINE_TX_QUEUE`에 저장됩니다.
- **IndexedDB:** 대용량 데이터나 이미지(다이어리 사진) 등은 IndexedDB를 통해 오프라인 상황에서도 안전하게 보관됩니다.
- **Auto Flush:** 브라우저가 다시 온라인 상태가 되면(`online` 이벤트) 대기 중인 큐를 Supabase로 일괄 전송합니다.

<!-- Claude (2026-04-30): 큐 일관성 결함
  - 다이어리는 enqueueWithSync(IDB)로 들어가지만, setShared('diaries')는 quota 초과 시
    LocalStorage offlineQueue에 enqueue 시도 → 사진 포함 시 5MB 한도 초과로 enqueue 자체가 실패
  - enqueueOffline은 QuotaExceededError를 console.error만 하고 swallow → 사용자는 저장된 줄 알지만 실제 유실
  - 권장: setShared 내부에서 'diaries' key는 LocalStorage 큐 우회하고 IDB 큐만 사용,
          enqueue 실패 시 toast로 명시적 경고 -->

### 2.3 위젯 기반 홈 화면
- **Customizable UI:** `react-grid-layout`을 사용하여 사용자가 홈 화면의 위젯(잔액 요약, 페이스 예측, AI 코치 등)을 자유롭게 배치하고 크기를 조절할 수 있습니다.
- **AI Nudge:** `/api/nudge` 엔드포인트를 통해 사용자의 소비 패턴을 분석하고 맞춤형 조언을 제공합니다.

---

## 3. 주요 모듈 및 파일 상세 (Detailed File Analysis)

### 3.1 App.jsx (Main Controller)
- 애플리케이션의 핵심 로직이 집약된 파일입니다 (약 1,150행).
- 라우팅, 전역 상태 정의, 데이터 로드/저장 로직, 오프라인 처리 로직을 포함합니다.
- `Suspense`와 `lazy`를 활용하여 20개 이상의 뷰를 효율적으로 로드합니다.

<!-- Claude (2026-04-30): _makeSetter 클로저 race 위험
  - App.jsx:611 `_makeSetter(key, rawSetter)`는 `let next` 외부 변수를 closure로 캡처
  - 같은 setter가 빠르게 두 번 호출되면 두 번째 호출의 next가 첫 번째 microtask 실행 전에 덮어씀
  - 즉 setDiaries(A) → setDiaries(B) 순으로 호출 시 setShared('diaries', B)가 두 번 큐잉되며 A는 사라짐
  - 권장: closure 변수 대신 functional update 결과를 인자로 직접 전달하거나,
          updater 결과를 ref + sequence id로 추적 -->

### 3.2 supabase.js (Database Utility)
- Supabase와의 모든 인터랙션을 캡슐화합니다.
- 단순 CRUD 외에도 RPC(Stored Procedures)를 호출하여 원자적 연산(예: 키즈 미션 보상 지급)을 수행합니다.

<!-- Claude (2026-04-30): save() 단일 upsert의 근본 한계
  - save()는 row 전체를 upsert하므로 diaries 한 건 추가에도 N개 사진 전부를 재전송
  - 트랜잭션이나 낙관적 락(updated_at 비교)이 없어 동시 수정 시 마지막 writer가 이전 변경을 덮어씀
  - DELETE 이벤트 핸들러도 `onUpdate(key, [])`로 모든 키를 빈 배열로 만들어버림 → diary row가 잠시라도 삭제되면 즉시 클라이언트가 비워짐
  - 권장: diaries 전용 테이블(diaries_v2) + insert/update/delete 분리 + version 컬럼 -->

### 3.3 InputSheet.jsx (Data Entry)
- 감성적인 '다이어리'와 실용적인 '지출 입력'을 결합한 인터페이스입니다.
- 입력 시 해당 카테고리의 남은 예산을 실시간으로 계산하여 시각적 피드백(`getRemainsTone`)을 제공합니다.

<!-- Claude (2026-04-30): 사진 처리 방어 부재
  - InputSheet.jsx:122 compressImage 실패 시 console.warn만 하고 무시 → 사용자는 실패 인지 불가
  - photos는 최대 2장으로 제한되지만 한 장당 최대 크기 검증이 없음
  - 권장: dataUrl.length 체크 후 200KB 초과 시 추가 압축, 실패 시 toast 알림 -->

---

## 4. 특이 사항 및 최적화 전략

- **PWA Share Target:** 외부 앱(SMS, 이미지)에서 공유된 데이터를 앱으로 직접 전달받아 처리하는 기능을 갖추고 있습니다 (`sw-share` 이벤트).
- **Lazy Loading for Transactions:** 지출 내역(`tx`)을 연도별로 분리하여 저장하고, 필요한 시점에만 로드(`loadTxYear`)함으로써 초기 로딩 속도를 개선했습니다.
- **Debounced Save:** 사용자 설정(이름, 예산 등)은 800ms의 디바운스를 적용하여 잦은 서버 요청을 방지합니다.

<!-- Claude (2026-04-30): 다이어리 디바운스 누락
  - setShared의 debounce 대상은 ["names","budgets","taxConfig"]로 한정 (App.jsx:524)
  - diaries는 즉시 저장 → 사진 첨부 후 빠르게 두 번 저장하면 두 upsert가 경쟁
  - 권장: diaries에도 300ms 디바운스 적용 + 직전 in-flight 요청 abort -->

## 5. 향후 개선 가능성 및 제언

- **SRP 준수:** `App.jsx`와 `BudgetView.jsx` 등 거대 파일들을 기능 단위(Hooks, Sub-components)로 더 세분화하여 유지보수성을 높일 수 있습니다.
- **TypeScript 도입:** 현재 JSDoc으로 타입을 관리하고 있으나, 전체 프로젝트를 `.ts/.tsx`로 전환하여 타입 안정성을 더욱 강화할 수 있습니다.
- **Unit Tests 확대:** `vitest` 환경이 구축되어 있으므로 핵심 비즈니스 로직(예산 계산, 오프라인 큐 처리)에 대한 테스트 코드를 보강하는 것을 권장합니다.

<!-- Claude (2026-04-30): 우선순위 추가 항목
  1. (P0) Realtime echo 무시 — 자기 자신이 발생시킨 broadcast를 client_id/timestamp로 식별
  2. (P0) updateSharedState의 null/undefined/빈 배열 가드 — 서버에서 비정상 값 수신 시 lastKnownGood 보존
  3. (P0) setShared 저장 전 lastKnownGood snapshot을 IDB에 보관 (실패 시 자동 복원)
  4. (P1) Supabase Storage 도입 — 사진은 buckets에 업로드 후 URL만 JSONB에 보관
  5. (P1) diaries 전용 테이블 + version 컬럼 + insert/update/delete API 분리
  6. (P2) 데이터 무결성 자가진단 — 부팅 시 diaries 길이 감소 감지하면 자동 백업 비교 -->
