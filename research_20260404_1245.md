# 💎 Budget-v2 프로젝트 심층 분석 보고서

> **작성일시**: 2026년 4월 4일 12:45
> **분석 대상**: `budget-v2` 프로젝트 코드베이스 전체

본 문서는 `budget-v2` (가정 경영 가계부 앱)의 폴더와 코드를 깊이 있게 분석하여 작동 원리, 아키텍처, 데이터 플로우 및 주요 세부 사항을 정리한 상세 보고서입니다.

---

## 1. 프로젝트 개요 및 기술 스택

이 프로젝트는 부부가 함께 사용하는 "가정 경영 가계부 앱"으로, 지출 내역 관리뿐만 아니라 카드 정산, 고정비/할부 관리, 연간 재무 계획, 자산 관리(순자산), 그리고 연말정산 최적화 기능까지 포괄하는 고도화된 웹 애플리케이션입니다.

- **프론트엔드**: React 18, Vite
- **상태 관리**: React Context API (`BudgetContext`) + 전역 `useState`
- **데이터베이스/동기화**: Supabase (PostgreSQL + Realtime 제어)
- **서버리스 함수(Vercel)**: Claude API를 활용한 OCR 영수증 스캔 (`api/ocr.js`) 및 AI 예산 배분 (`api/budget-ai.js`)
- **스타일링**: 순수 CSS + JavaScript 객체 토큰 (`THEME_TOKENS`) / 다크, 라이트 모드 지원

---

## 2. 아키텍처 및 데이터 플로우

### 2.1 단일 진실 공급원 (App.jsx)
앱의 모든 핵심 생명주기와 전역 상태(State)는 루트 컴포넌트인 `App.jsx`가 관장합니다.

1. **로컬 설정(Private State)**: `sliderCfg`, `theme`, `householdId`, `myRole` 등 본인 기기에만 한정적으로 적용되는 설정값은 `localStorage`에 저장됩니다.
2. **공유 상태(Shared State)**: `tx`(거래내역), `fixed`(고정비), `install`(할부), `cards`(카드), `assets`(자산), `plan`(연간계획), `budgets`(예산), `names`(사용자 호칭), `taxConfig`(세금 설정) 등은 모두 Supabase의 `household_data` 테이블을 통해 동기화됩니다.

### 2.2 동기화 메커니즘 (`setShared` 래퍼)
데이터 정합성을 유지하면서도 부드러운 UX(낙관적 업데이트)를 제공하기 위해, 모든 상태 변경은 `setShared` 함수(또는 그 파생 함수들)를 거칩니다.

```javascript
// 동작 원리 요약
1. 사용자가 UI 조작 트리거 (ex: 지출 추가)
2. setShared(key, value, rawSetter) 호출
3. rawSetter(value) 즉시 실행 → 화면(UI) 즉시 변경 (Optimistic UI)
4. validate(key, value) → 스키마 사전 검증 (에러 방지)
5. navigator.onLine 확인 → 오프라인이면 offlineQueue에 적재
6. db.save(householdId, key, value) → 비동기로 Supabase 에 upsert (지수 백오프 3회 재시도 로직 포함)
7. 정상 시 syncStatus = "ok", 실패 시 큐에 적재 및 "error" 상태 표시
```

### 2.3 실시간 양방향 통신 (Supabase Realtime)
한 기기에서 `db.save`로 데이터가 upsert되면, 파트너 기기에서는 `db.subscribe`를 통해 실시간 브로드캐스트 이벤트를 수신합니다. 수신된 이벤트는 `updateSharedState` 콜백을 통해 로컬 상태를 강제로 동기화합니다. 이를 통해 완전히 실시간 연동되는 가계부 경험을 제공합니다.

---

## 3. 핵심 데이터 구조 (Data Models)

Supabase는 Key-Value 형태 혹은 JSONB로 데이터를 담으며, 내부 객체들은 다음과 같은 스키마 구조를 가집니다.

- **`tx` (다건의 거래 내역)**: `id`, `amount`, `cat`(카테고리), `date`, `memo`, `type`(수입/지출/이체), `who`(사용자), `cardId`
  - *디테일*: 데이터가 너무 비대해지는 것을 막기 위해 `tx_2024`, `tx_2025` 처럼 연도별로 Lazy Loading(지연 로딩) 방식으로 개선됨. 현재 연도만 초기 로딩하고 과거는 필요시 로드.
- **`cards` (카드 관리)**: `id`, `name`(또는 label), `icon`, `color`, `billingStartDay`, `billingEndDay`, `paymentDay` (카드 정산 알고리즘 연동용)
- **`install` (할부 항목)**: `id`, `name`, `total`(전체금액), `month`(할부개월), `monthly`(월납입액), `paidMonths`(현재 납부된 개월 수)
- **`plan` (연간계획 및 재무)**: `salary`(각 사용자 급여), `savingsTarget`, `yearSpendLimit`, `events` 등의 구조를 포함하는 객체. 단일 진실 공급원(SSOT) 형태로 정리되어, 과거 흩어져있던(`monthlyIncome` 등) 속성들을 `plan.salary` 하위로 마이그레이션하여 모아둠.

---

## 4. 폴더 및 파일 구성 상세

### `src/views/` (주요 화면 단위)
- **`App.jsx`**: 전체 라우팅, 전역 상태, Supabase 동기화 통제 보드
- **`HomeView.jsx`**: 메인 대시보드 (당월 남은 예산, 이번 달 지출 요약, PACE 오버레이 등)
- **`EntryView.jsx`**: 내역 수동 입력 및 영수증을 카메라로 찍어 OCR 분석하는 메뉴 (`InputModal` 연동)
- **`ReportView.jsx` / `CalendarView.jsx` / `PlanView.jsx` / `TaxOptimizerView.jsx`**:
  - `ReportView` 내에 여러 하위 탭이 존재. 통계, 캘린더 히트맵, 재무 플랜 기획, 연말정산 최적화를 수행함. `useMemo` 처리를 통해 거대한 `tx` 배열 파싱 시 성능 저하를 방지.
- **`FixedView.jsx` / `CardView.jsx`**: 고정비, 할부 관리, 카드 관리. 카드의 이용기간을 계산해서 캘린더 렌더링에 필요한 `getBillingPeriod` 훅들을 지원함.
- **`AssetView.jsx`**: 자산(계좌, 현금, 투자, 대출) 관리표

### `src/utils/` (핵심 비즈니스 로직)
- **`supabase.js`**: `db.save`, `db.load`, `db.subscribe` 등을 래핑하여 추상화한 데이터베이스 접근 레이어
- **`offlineQueue.js`**: 오프라인 상태일 때 입력한 데이터를 `localStorage` 큐에 쌓아두고 인터넷 복구 시 자동 `flush`하게 하는 유틸리티
- **`helpers.js`, `validate.js`**: 결제 기준일 계산 등 복잡한 도메인 로직 및 저장 전 데이터 스키마 검증기

---

## 5. 최신 코드 변경점과 극복한 기술적 허들

프로젝트 최근 고도화 이력(`PLAN_v3`, `CODE_ANALYSIS_V2` 기반)을 통해 앱이 한 단계 진화했음을 알 수 있습니다.

1. **Prop Drilling의 해소**: 기존 `App.jsx`에서 모든 View 컴포넌트로 수많은 Props를 내려주던 비효율을 `BudgetContext`라는 Context API를 신규 도입함으로써 구조적으로 개선 중입니다.
2. **반응성 최적화 (useMemo)**: 월별 지출 합산 등 빈번히 일어나는 Array Helper(`filter`, `reduce`)에 `useMemo`를 적용하여 렌더링마다 발생하는 병목을 제거했습니다.
3. **Lazy Loading과 데이터 마이그레이션**: 이전까지 단일 배열 상성에 의존하던 거래내역(`tx`)을 초기 로드 단계에서 `tx_{YYYY}` 연도별로 찢어서(Migration) 저장/호출하도록 바꿈으로써 장기 사용 시 발생할 메모리 한계를 타파했습니다.
4. **오프라인 큐 확보**: 네트워크 단절 시에도 로컬에 지출을 기입하고, 다시 켜지면 `flushOfflineQueue`가 실행되도록 한 PWA 수준의 안정성.
5. **동적 날짜 전환 로직 결함 패치**: 모듈이 로드되는 순간 앱이 켜진 시점의 상수로 고정되던 `NOW`, `DAY` 등의 변수를 `getYear()`, `getMonth()` 등의 함수형(`() => new Date()...`)으로 전환하여 앱을 재시작하지 않아도 캘린더나 일일 통계가 정상 전환되도록 고쳤습니다.

---

## 6. 잠재적인 오류 구조 및 향후 과제

현재 남은 과제는 데이터 스키마 불일치로 인한 사소한 필드 참조 오류들입니다:
- **`card` 필드명의 혼재**: `CardView`는 `name`이라는 필드로 저장하고 있으나, 문서상에는 `label`로 다루려는 시도가 관찰됨. `EntryView`나 `TxEditModal` 등에서 이 부분의 단일화 작업이 중요합니다.
- **`install`의 개월수/총액 필드 표기 혼란**: 할부 개월수를 `month`로 저장하고 UI 계산부에서는 `months`를 읽으려 하는 등 오타 기반 버그들이 존재하여 이의 일괄적인 수정(또는 `monthly` 필드 참조로 우회)을 지향해야 합니다.

### 총평
`budget-v2`는 Vercel과 Supabase를 매끄럽게 브릿지하여 높은 수준의 '낙관적 렌더링 + 실시간 상태 동기화'를 달성한 완성도 높은 아키텍처를 보유하고 있습니다. 향후 컴포넌트 모듈화 및 Context API 전면 이식만 완결된다면 무결점의 가계부 사스(SaaS)로 기능할 것입니다.
